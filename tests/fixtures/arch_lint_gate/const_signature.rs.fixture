struct Error;
struct Problem<const N: usize>;

async fn served(_: [u8; { 1 + 1 }]) -> Result<[u8; { 2 + 2 }], Error>
where
    Error: From<Problem<{ 3 + 3 }>>,
{
    let _ = std::fs::read("inside-real-body.redb");
}
