"""(d) Connector packs: realistic well-formed packs and one planted defect per rule.

The wire shape and digests follow PACK-IMPORT-DESIGN §3.1 (normative); the rules
and violation codes follow §3.10.1. `spec` holds the editable entry list,
`assemble` lays out the archive and computes every digest, `generate` builds
size-realistic packs and `malformed_*` plant one defect per validation rule.
"""
