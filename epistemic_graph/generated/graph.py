# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated graph engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import OpaqueResult


class AddNodeRequest(BaseModel):
    """Request body for AddNode - contract/schemas/method.request.json#/methods/AddNode."""

    model_config = ConfigDict(extra="forbid")

    node_id: str
    properties_msgpack: bytes


async def send_add_node(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """AddNode - node:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    AddNodeRequest.model_validate(params or {})
    payload = await client._send(
        "AddNode",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("AddNode", payload)


class CreateNodeIfAbsentRequest(BaseModel):
    """Request body for CreateNodeIfAbsent - contract/schemas/method.request.json#/methods/CreateNodeIfAbsent."""

    model_config = ConfigDict(extra="forbid")

    node_id: str
    properties_msgpack: bytes


async def send_create_node_if_absent(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """CreateNodeIfAbsent - node:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CreateNodeIfAbsentRequest.model_validate(params or {})
    payload = await client._send(
        "CreateNodeIfAbsent",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class RemoveNodeRequest(BaseModel):
    """Request body for RemoveNode - contract/schemas/method.request.json#/methods/RemoveNode."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_remove_node(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RemoveNode - node:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RemoveNodeRequest.model_validate(params or {})
    payload = await client._send(
        "RemoveNode",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RemoveNode", payload)


class HasNodeRequest(BaseModel):
    """Request body for HasNode - contract/schemas/method.request.json#/methods/HasNode."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_has_node(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """HasNode - node:read, None, replay NotReplayable.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    HasNodeRequest.model_validate(params or {})
    payload = await client._send(
        "HasNode",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetNodesRequest(BaseModel):
    """Request body for GetNodes - contract/schemas/method.request.json#/methods/GetNodes."""

    model_config = ConfigDict(extra="forbid")

    # Method::GetNodes carries no request fields.


async def send_get_nodes(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[tuple[str, Any]]:
    """GetNodes - node:read, None, replay NotReplayable.

    Result: typed ResultPayload::NodeList (contract/schemas/result.NodeList.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetNodesRequest.model_validate(params or {})
    payload = await client._send(
        "GetNodes",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetNodesByLabelRequest(BaseModel):
    """Request body for GetNodesByLabel - contract/schemas/method.request.json#/methods/GetNodesByLabel."""

    model_config = ConfigDict(extra="forbid")

    after: Any | None = None
    label: str
    limit: int


async def send_get_nodes_by_label(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[tuple[str, Any]]:
    """GetNodesByLabel - node:read, None, replay NotReplayable.

    Result: typed ResultPayload::NodeList (contract/schemas/result.NodeList.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetNodesByLabelRequest.model_validate(params or {})
    payload = await client._send(
        "GetNodesByLabel",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetNodePropertiesRequest(BaseModel):
    """Request body for GetNodeProperties - contract/schemas/method.request.json#/methods/GetNodeProperties."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_get_node_properties(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetNodeProperties - node:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetNodePropertiesRequest.model_validate(params or {})
    payload = await client._send(
        "GetNodeProperties",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetNodeProperties", payload)


class CompareAndSetNodeFieldsRequest(BaseModel):
    """Request body for CompareAndSetNodeFields - contract/schemas/method.request.json#/methods/CompareAndSetNodeFields."""

    model_config = ConfigDict(extra="forbid")

    conditions_msgpack: bytes
    node_id: str
    updates_msgpack: bytes


async def send_compare_and_set_node_fields(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """CompareAndSetNodeFields - node:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CompareAndSetNodeFieldsRequest.model_validate(params or {})
    payload = await client._send(
        "CompareAndSetNodeFields",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class CreateSummaryNodeRequest(BaseModel):
    """Request body for CreateSummaryNode - contract/schemas/method.request.json#/methods/CreateSummaryNode."""

    model_config = ConfigDict(extra="forbid")

    child_ids: list[str]
    level: int
    props_msgpack: bytes


async def send_create_summary_node(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """CreateSummaryNode - memory:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CreateSummaryNodeRequest.model_validate(params or {})
    payload = await client._send(
        "CreateSummaryNode",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class ConsolidateRequest(BaseModel):
    """Request body for Consolidate - contract/schemas/method.request.json#/methods/Consolidate."""

    model_config = ConfigDict(extra="forbid")

    episodic_ids: list[str]
    semantic_props_msgpack: bytes


async def send_consolidate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """Consolidate - memory:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ConsolidateRequest.model_validate(params or {})
    payload = await client._send(
        "Consolidate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class ReinforceRequest(BaseModel):
    """Request body for Reinforce - contract/schemas/method.request.json#/methods/Reinforce."""

    model_config = ConfigDict(extra="forbid")

    node_id: str
    now_ms: int
    weight: float


async def send_reinforce(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """Reinforce - memory:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReinforceRequest.model_validate(params or {})
    payload = await client._send(
        "Reinforce",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class DecayNodeRequest(BaseModel):
    """Request body for DecayNode - contract/schemas/method.request.json#/methods/DecayNode."""

    model_config = ConfigDict(extra="forbid")

    half_life_ms: int
    node_id: str
    now_ms: int


async def send_decay_node(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """DecayNode - memory:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DecayNodeRequest.model_validate(params or {})
    payload = await client._send(
        "DecayNode",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class DecayMemoriesRequest(BaseModel):
    """Request body for DecayMemories - contract/schemas/method.request.json#/methods/DecayMemories."""

    model_config = ConfigDict(extra="forbid")

    half_life_ms: int
    ids: list[str]
    now_ms: int


async def send_decay_memories(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """DecayMemories - memory:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DecayMemoriesRequest.model_validate(params or {})
    payload = await client._send(
        "DecayMemories",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class EvictBelowRequest(BaseModel):
    """Request body for EvictBelow - contract/schemas/method.request.json#/methods/EvictBelow."""

    model_config = ConfigDict(extra="forbid")

    delete: bool
    ids: list[str]
    threshold: float


async def send_evict_below(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """EvictBelow - memory:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    EvictBelowRequest.model_validate(params or {})
    payload = await client._send(
        "EvictBelow",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class MaintainRequest(BaseModel):
    """Request body for Maintain - contract/schemas/method.request.json#/methods/Maintain."""

    model_config = ConfigDict(extra="forbid")

    delete: bool
    evict_threshold: float
    half_life_ms: int
    ids: list[str]
    now_ms: int


async def send_maintain(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Maintain - memory:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MaintainRequest.model_validate(params or {})
    payload = await client._send(
        "Maintain",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Maintain", payload)


class SummaryChildrenRequest(BaseModel):
    """Request body for SummaryChildren - contract/schemas/method.request.json#/methods/SummaryChildren."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_summary_children(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """SummaryChildren - memory:read, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    SummaryChildrenRequest.model_validate(params or {})
    payload = await client._send(
        "SummaryChildren",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class SummariesAtLevelRequest(BaseModel):
    """Request body for SummariesAtLevel - contract/schemas/method.request.json#/methods/SummariesAtLevel."""

    model_config = ConfigDict(extra="forbid")

    level: int


async def send_summaries_at_level(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """SummariesAtLevel - memory:read, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    SummariesAtLevelRequest.model_validate(params or {})
    payload = await client._send(
        "SummariesAtLevel",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class StartTrajectoryRequest(BaseModel):
    """Request body for StartTrajectory - contract/schemas/method.request.json#/methods/StartTrajectory."""

    model_config = ConfigDict(extra="forbid")

    props_msgpack: bytes


async def send_start_trajectory(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """StartTrajectory - memory:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    StartTrajectoryRequest.model_validate(params or {})
    payload = await client._send(
        "StartTrajectory",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class AppendStepRequest(BaseModel):
    """Request body for AppendStep - contract/schemas/method.request.json#/methods/AppendStep."""

    model_config = ConfigDict(extra="forbid")

    action_msgpack: bytes
    next_state_ref: Any | None = None
    reward: float
    state_ref: Any | None = None
    t: int
    traj_id: str


async def send_append_step(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """AppendStep - memory:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    AppendStepRequest.model_validate(params or {})
    payload = await client._send(
        "AppendStep",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("AppendStep", payload)


class DiscountedReturnRequest(BaseModel):
    """Request body for DiscountedReturn - contract/schemas/method.request.json#/methods/DiscountedReturn."""

    model_config = ConfigDict(extra="forbid")

    gamma: float
    traj_id: str


async def send_discounted_return(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """DiscountedReturn - memory:read, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DiscountedReturnRequest.model_validate(params or {})
    payload = await client._send(
        "DiscountedReturn",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BestTrajectoryRequest(BaseModel):
    """Request body for BestTrajectory - contract/schemas/method.request.json#/methods/BestTrajectory."""

    model_config = ConfigDict(extra="forbid")

    gamma: float
    traj_ids: list[str]


async def send_best_trajectory(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BestTrajectory - memory:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    BestTrajectoryRequest.model_validate(params or {})
    payload = await client._send(
        "BestTrajectory",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BestTrajectory", payload)


class GetNodePropertiesBatchRequest(BaseModel):
    """Request body for GetNodePropertiesBatch - contract/schemas/method.request.json#/methods/GetNodePropertiesBatch."""

    model_config = ConfigDict(extra="forbid")

    node_ids: list[str]


async def send_get_node_properties_batch(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetNodePropertiesBatch - node:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetNodePropertiesBatchRequest.model_validate(params or {})
    payload = await client._send(
        "GetNodePropertiesBatch",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetNodePropertiesBatch", payload)


class HasNodesBatchRequest(BaseModel):
    """Request body for HasNodesBatch - contract/schemas/method.request.json#/methods/HasNodesBatch."""

    model_config = ConfigDict(extra="forbid")

    node_ids: list[str]


async def send_has_nodes_batch(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """HasNodesBatch - node:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    HasNodesBatchRequest.model_validate(params or {})
    payload = await client._send(
        "HasNodesBatch",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("HasNodesBatch", payload)


class NodeCountRequest(BaseModel):
    """Request body for NodeCount - contract/schemas/method.request.json#/methods/NodeCount."""

    model_config = ConfigDict(extra="forbid")

    # Method::NodeCount carries no request fields.


async def send_node_count(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """NodeCount - node:read, None, replay NotReplayable.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    NodeCountRequest.model_validate(params or {})
    payload = await client._send(
        "NodeCount",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class NodeIdsRequest(BaseModel):
    """Request body for NodeIds - contract/schemas/method.request.json#/methods/NodeIds."""

    model_config = ConfigDict(extra="forbid")

    # Method::NodeIds carries no request fields.


async def send_node_ids(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """NodeIds - node:read, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    NodeIdsRequest.model_validate(params or {})
    payload = await client._send(
        "NodeIds",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class AddEdgeRequest(BaseModel):
    """Request body for AddEdge - contract/schemas/method.request.json#/methods/AddEdge."""

    model_config = ConfigDict(extra="forbid")

    properties_msgpack: bytes
    source_id: str
    target_id: str


async def send_add_edge(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """AddEdge - edge:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    AddEdgeRequest.model_validate(params or {})
    payload = await client._send(
        "AddEdge",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class RemoveEdgeRequest(BaseModel):
    """Request body for RemoveEdge - contract/schemas/method.request.json#/methods/RemoveEdge."""

    model_config = ConfigDict(extra="forbid")

    source_id: str
    target_id: str


async def send_remove_edge(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RemoveEdge - edge:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RemoveEdgeRequest.model_validate(params or {})
    payload = await client._send(
        "RemoveEdge",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RemoveEdge", payload)


class InvalidateEdgeRequest(BaseModel):
    """Request body for InvalidateEdge - contract/schemas/method.request.json#/methods/InvalidateEdge."""

    model_config = ConfigDict(extra="forbid")

    invalid_at: int
    relationship: str
    source_id: str
    target_id: str
    tx_now: int


async def send_invalidate_edge(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """InvalidateEdge - edge:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    InvalidateEdgeRequest.model_validate(params or {})
    payload = await client._send(
        "InvalidateEdge",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class SupersedeEdgeRequest(BaseModel):
    """Request body for SupersedeEdge - contract/schemas/method.request.json#/methods/SupersedeEdge."""

    model_config = ConfigDict(extra="forbid")

    prior_relationship: str
    prior_source: str
    prior_target: str
    properties_msgpack: bytes
    source_id: str
    target_id: str
    tx_now: int
    valid_at: int


async def send_supersede_edge(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """SupersedeEdge - edge:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    SupersedeEdgeRequest.model_validate(params or {})
    payload = await client._send(
        "SupersedeEdge",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("SupersedeEdge", payload)


class HasEdgeRequest(BaseModel):
    """Request body for HasEdge - contract/schemas/method.request.json#/methods/HasEdge."""

    model_config = ConfigDict(extra="forbid")

    source_id: str
    target_id: str


async def send_has_edge(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """HasEdge - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    HasEdgeRequest.model_validate(params or {})
    payload = await client._send(
        "HasEdge",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetEdgesRequest(BaseModel):
    """Request body for GetEdges - contract/schemas/method.request.json#/methods/GetEdges."""

    model_config = ConfigDict(extra="forbid")

    # Method::GetEdges carries no request fields.


async def send_get_edges(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[tuple[str, str, bytes]]:
    """GetEdges - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::EdgeList (contract/schemas/result.EdgeList.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetEdgesRequest.model_validate(params or {})
    payload = await client._send(
        "GetEdges",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetEdgesPageRequest(BaseModel):
    """Request body for GetEdgesPage - contract/schemas/method.request.json#/methods/GetEdgesPage."""

    model_config = ConfigDict(extra="forbid")

    after: Any | None = None
    limit: int


async def send_get_edges_page(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetEdgesPage - edge:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetEdgesPageRequest.model_validate(params or {})
    payload = await client._send(
        "GetEdgesPage",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetEdgesPage", payload)


class ClearGraphRequest(BaseModel):
    """Request body for ClearGraph - contract/schemas/method.request.json#/methods/ClearGraph."""

    model_config = ConfigDict(extra="forbid")

    # Method::ClearGraph carries no request fields.


async def send_clear_graph(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """ClearGraph - graph:admin, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ClearGraphRequest.model_validate(params or {})
    payload = await client._send(
        "ClearGraph",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetEdgePropertiesRequest(BaseModel):
    """Request body for GetEdgeProperties - contract/schemas/method.request.json#/methods/GetEdgeProperties."""

    model_config = ConfigDict(extra="forbid")

    source_id: str
    target_id: str


async def send_get_edge_properties(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetEdgeProperties - edge:read, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetEdgePropertiesRequest.model_validate(params or {})
    payload = await client._send(
        "GetEdgeProperties",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetEdgeProperties", payload)


class GetEdgePropertiesBatchRequest(BaseModel):
    """Request body for GetEdgePropertiesBatch - contract/schemas/method.request.json#/methods/GetEdgePropertiesBatch."""

    model_config = ConfigDict(extra="forbid")

    edges: list[list[Any]]


async def send_get_edge_properties_batch(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetEdgePropertiesBatch - edge:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetEdgePropertiesBatchRequest.model_validate(params or {})
    payload = await client._send(
        "GetEdgePropertiesBatch",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetEdgePropertiesBatch", payload)


class EdgeCountRequest(BaseModel):
    """Request body for EdgeCount - contract/schemas/method.request.json#/methods/EdgeCount."""

    model_config = ConfigDict(extra="forbid")

    # Method::EdgeCount carries no request fields.


async def send_edge_count(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """EdgeCount - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    EdgeCountRequest.model_validate(params or {})
    payload = await client._send(
        "EdgeCount",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class InDegreeRequest(BaseModel):
    """Request body for InDegree - contract/schemas/method.request.json#/methods/InDegree."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_in_degree(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """InDegree - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    InDegreeRequest.model_validate(params or {})
    payload = await client._send(
        "InDegree",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class OutDegreeRequest(BaseModel):
    """Request body for OutDegree - contract/schemas/method.request.json#/methods/OutDegree."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_out_degree(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """OutDegree - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    OutDegreeRequest.model_validate(params or {})
    payload = await client._send(
        "OutDegree",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetPredecessorsRequest(BaseModel):
    """Request body for GetPredecessors - contract/schemas/method.request.json#/methods/GetPredecessors."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_get_predecessors(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """GetPredecessors - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetPredecessorsRequest.model_validate(params or {})
    payload = await client._send(
        "GetPredecessors",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetSuccessorsRequest(BaseModel):
    """Request body for GetSuccessors - contract/schemas/method.request.json#/methods/GetSuccessors."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_get_successors(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """GetSuccessors - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetSuccessorsRequest.model_validate(params or {})
    payload = await client._send(
        "GetSuccessors",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetNeighborsRequest(BaseModel):
    """Request body for GetNeighbors - contract/schemas/method.request.json#/methods/GetNeighbors."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_get_neighbors(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """GetNeighbors - edge:read, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetNeighborsRequest.model_validate(params or {})
    payload = await client._send(
        "GetNeighbors",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetNeighborsBatchRequest(BaseModel):
    """Request body for GetNeighborsBatch - contract/schemas/method.request.json#/methods/GetNeighborsBatch."""

    model_config = ConfigDict(extra="forbid")

    node_ids: list[str]


async def send_get_neighbors_batch(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetNeighborsBatch - edge:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetNeighborsBatchRequest.model_validate(params or {})
    payload = await client._send(
        "GetNeighborsBatch",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetNeighborsBatch", payload)


class UnionGetNodePropertiesRequest(BaseModel):
    """Request body for UnionGetNodeProperties - contract/schemas/method.request.json#/methods/UnionGetNodeProperties."""

    model_config = ConfigDict(extra="forbid")

    graphs: list[str]
    node_id: str


async def send_union_get_node_properties(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """UnionGetNodeProperties - node:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    UnionGetNodePropertiesRequest.model_validate(params or {})
    payload = await client._send(
        "UnionGetNodeProperties",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("UnionGetNodeProperties", payload)


class UnionGetNodesByLabelRequest(BaseModel):
    """Request body for UnionGetNodesByLabel - contract/schemas/method.request.json#/methods/UnionGetNodesByLabel."""

    model_config = ConfigDict(extra="forbid")

    graphs: list[str]
    label: str
    limit: int


async def send_union_get_nodes_by_label(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """UnionGetNodesByLabel - node:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    UnionGetNodesByLabelRequest.model_validate(params or {})
    payload = await client._send(
        "UnionGetNodesByLabel",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("UnionGetNodesByLabel", payload)


class UnionGetNeighborsRequest(BaseModel):
    """Request body for UnionGetNeighbors - contract/schemas/method.request.json#/methods/UnionGetNeighbors."""

    model_config = ConfigDict(extra="forbid")

    graphs: list[str]
    node_id: str


async def send_union_get_neighbors(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """UnionGetNeighbors - node:read, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    UnionGetNeighborsRequest.model_validate(params or {})
    payload = await client._send(
        "UnionGetNeighbors",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class PruneByLifecycleRequest(BaseModel):
    """Request body for PruneByLifecycle - contract/schemas/method.request.json#/methods/PruneByLifecycle."""

    model_config = ConfigDict(extra="forbid")

    max_age_secs: int
    min_score: float


async def send_prune_by_lifecycle(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PruneByLifecycle - node:admin, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PruneByLifecycleRequest.model_validate(params or {})
    payload = await client._send(
        "PruneByLifecycle",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PruneByLifecycle", payload)


class MetricsRequest(BaseModel):
    """Request body for Metrics - contract/schemas/method.request.json#/methods/Metrics."""

    model_config = ConfigDict(extra="forbid")

    # Method::Metrics carries no request fields.


async def send_metrics(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Metrics - service:control, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    MetricsRequest.model_validate(params or {})
    payload = await client._send(
        "Metrics",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Metrics", payload)


class EvictLRURequest(BaseModel):
    """Request body for EvictLRU - contract/schemas/method.request.json#/methods/EvictLRU."""

    model_config = ConfigDict(extra="forbid")

    max_nodes: int


async def send_evict_l_r_u(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """EvictLRU - node:admin, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    EvictLRURequest.model_validate(params or {})
    payload = await client._send(
        "EvictLRU",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class DecaySweepRequest(BaseModel):
    """Request body for DecaySweep - contract/schemas/method.request.json#/methods/DecaySweep."""

    model_config = ConfigDict(extra="forbid")

    floor: float
    half_life_secs: float
    prune: bool


async def send_decay_sweep(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DecaySweep - node:admin, GraphRedb, replay OperationIdentity.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DecaySweepRequest.model_validate(params or {})
    payload = await client._send(
        "DecaySweep",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DecaySweep", payload)


class TouchNodesRequest(BaseModel):
    """Request body for TouchNodes - contract/schemas/method.request.json#/methods/TouchNodes."""

    model_config = ConfigDict(extra="forbid")

    node_ids: list[str]


async def send_touch_nodes(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """TouchNodes - node:admin, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TouchNodesRequest.model_validate(params or {})
    payload = await client._send(
        "TouchNodes",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class GetSubgraphRequest(BaseModel):
    """Request body for GetSubgraph - contract/schemas/method.request.json#/methods/GetSubgraph."""

    model_config = ConfigDict(extra="forbid")

    node_ids: list[str]


async def send_get_subgraph(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetSubgraph - node:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetSubgraphRequest.model_validate(params or {})
    payload = await client._send(
        "GetSubgraph",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetSubgraph", payload)


class ForkRequest(BaseModel):
    """Request body for Fork - contract/schemas/method.request.json#/methods/Fork."""

    model_config = ConfigDict(extra="forbid")

    # Method::Fork carries no request fields.


async def send_fork(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Fork - graph:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ForkRequest.model_validate(params or {})
    payload = await client._send(
        "Fork",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Fork", payload)


class DiffAgainstRequest(BaseModel):
    """Request body for DiffAgainst - contract/schemas/method.request.json#/methods/DiffAgainst."""

    model_config = ConfigDict(extra="forbid")

    other_graph: str


async def send_diff_against(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DiffAgainst - graph:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DiffAgainstRequest.model_validate(params or {})
    payload = await client._send(
        "DiffAgainst",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DiffAgainst", payload)


class ReconcileRequest(BaseModel):
    """Request body for Reconcile - contract/schemas/method.request.json#/methods/Reconcile."""

    model_config = ConfigDict(extra="forbid")

    graph_name: str
    msgpack: bytes


async def send_reconcile(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """Reconcile - graph:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReconcileRequest.model_validate(params or {})
    payload = await client._send(
        "Reconcile",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class ApplyMutationRequest(BaseModel):
    """Request body for ApplyMutation - contract/schemas/method.request.json#/methods/ApplyMutation."""

    model_config = ConfigDict(extra="forbid")

    event_type: str
    query: str


async def send_apply_mutation(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """ApplyMutation - graph:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ApplyMutationRequest.model_validate(params or {})
    payload = await client._send(
        "ApplyMutation",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class AddTriplesRequest(BaseModel):
    """Request body for AddTriples - contract/schemas/method.request.json#/methods/AddTriples."""

    model_config = ConfigDict(extra="forbid")

    ntriples: str | None = None
    turtle: str | None = None


async def send_add_triples(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """AddTriples - rdf:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    AddTriplesRequest.model_validate(params or {})
    payload = await client._send(
        "AddTriples",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("AddTriples", payload)


class RemoveTriplesRequest(BaseModel):
    """Request body for RemoveTriples - contract/schemas/method.request.json#/methods/RemoveTriples."""

    model_config = ConfigDict(extra="forbid")

    ntriples: str | None = None
    turtle: str | None = None


async def send_remove_triples(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RemoveTriples - rdf:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RemoveTriplesRequest.model_validate(params or {})
    payload = await client._send(
        "RemoveTriples",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RemoveTriples", payload)


class DropNamedGraphRequest(BaseModel):
    """Request body for DropNamedGraph - contract/schemas/method.request.json#/methods/DropNamedGraph."""

    model_config = ConfigDict(extra="forbid")

    # Method::DropNamedGraph carries no request fields.


async def send_drop_named_graph(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """DropNamedGraph - rdf:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DropNamedGraphRequest.model_validate(params or {})
    payload = await client._send(
        "DropNamedGraph",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload
