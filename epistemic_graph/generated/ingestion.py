# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated ingestion engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import (
    OpaqueResult,
    expect_string,
)


class ServedModalityRequest(BaseModel):
    """Request body for ServedModality - contract/schemas/method.request.json#/methods/ServedModality."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_served_modality(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ServedModality - modality:write, GraphRedb, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ServedModalityRequest.model_validate(params or {})
    payload = await client._send(
        "ServedModality",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ServedModality", payload)


class ParseFileRequest(BaseModel):
    """Request body for ParseFile - contract/schemas/method.request.json#/methods/ParseFile."""

    model_config = ConfigDict(extra="forbid")

    file_path: str
    source: bytes


async def send_parse_file(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ParseFile - compute:parse, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ParseFileRequest.model_validate(params or {})
    payload = await client._send(
        "ParseFile",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ParseFile", payload)


class ParseFilesRequest(BaseModel):
    """Request body for ParseFiles - contract/schemas/method.request.json#/methods/ParseFiles."""

    model_config = ConfigDict(extra="forbid")

    files_msgpack: bytes


async def send_parse_files(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ParseFiles - compute:parse, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ParseFilesRequest.model_validate(params or {})
    payload = await client._send(
        "ParseFiles",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ParseFiles", payload)


class IndexRepositoryRequest(BaseModel):
    """Request body for IndexRepository - contract/schemas/method.request.json#/methods/IndexRepository."""

    model_config = ConfigDict(extra="forbid")

    files_msgpack: bytes


async def send_index_repository(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """IndexRepository - compute:parse, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    IndexRepositoryRequest.model_validate(params or {})
    payload = await client._send(
        "IndexRepository",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("IndexRepository", payload)


class ObserveScreenRequest(BaseModel):
    """Request body for ObserveScreen - contract/schemas/method.request.json#/methods/ObserveScreen."""

    model_config = ConfigDict(extra="forbid")

    obs_msgpack: bytes


async def send_observe_screen(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ObserveScreen - compute:vision, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ObserveScreenRequest.model_validate(params or {})
    payload = await client._send(
        "ObserveScreen",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ObserveScreen", payload)


class AddEmbeddingRequest(BaseModel):
    """Request body for AddEmbedding - contract/schemas/method.request.json#/methods/AddEmbedding."""

    model_config = ConfigDict(extra="forbid")

    embedding: list[float]
    node_id: str


async def send_add_embedding(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """AddEmbedding - node:write, GraphRedb, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.ingestion.json#/methods/AddEmbedding).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    AddEmbeddingRequest.model_validate(params or {})
    payload = await client._send(
        "AddEmbedding",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("AddEmbedding", payload)


class SemanticIndexRequest(BaseModel):
    """Request body for SemanticIndex - contract/schemas/method.request.json#/methods/SemanticIndex."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_semantic_index(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """SemanticIndex - semantic:binding-write, SemanticIndexRedb, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    SemanticIndexRequest.model_validate(params or {})
    payload = await client._send(
        "SemanticIndex",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("SemanticIndex", payload)


class SemanticSearchRequest(BaseModel):
    """Request body for SemanticSearch - contract/schemas/method.request.json#/methods/SemanticSearch."""

    model_config = ConfigDict(extra="forbid")

    n_results: int
    query_embedding: list[float]


async def send_semantic_search(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """SemanticSearch - compute:semantic, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    SemanticSearchRequest.model_validate(params or {})
    payload = await client._send(
        "SemanticSearch",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("SemanticSearch", payload)


class DiscoverRequest(BaseModel):
    """Request body for Discover - contract/schemas/method.request.json#/methods/Discover."""

    model_config = ConfigDict(extra="forbid")

    k: int
    keywords: list[str]
    query_embedding: list[float]


async def send_discover(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Discover - compute:semantic, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DiscoverRequest.model_validate(params or {})
    payload = await client._send(
        "Discover",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Discover", payload)


class QuantumRequest(BaseModel):
    """Request body for Quantum - contract/schemas/method.request.json#/methods/Quantum."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_quantum(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Quantum - quantum:run, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    QuantumRequest.model_validate(params or {})
    payload = await client._send(
        "Quantum",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Quantum", payload)


class AsrRequest(BaseModel):
    """Request body for Asr - contract/schemas/method.request.json#/methods/Asr."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_asr(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Asr - asr:transcribe, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    AsrRequest.model_validate(params or {})
    payload = await client._send(
        "Asr",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Asr", payload)


class VizRequest(BaseModel):
    """Request body for Viz - contract/schemas/method.request.json#/methods/Viz."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_viz(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Viz - viz:render, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    VizRequest.model_validate(params or {})
    payload = await client._send(
        "Viz",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Viz", payload)
