# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated compute engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import (
    OpaqueResult,
    expect_count,
    expect_float,
    expect_ids,
)


class TopologicalSortRequest(BaseModel):
    """Request body for TopologicalSort - contract/schemas/method.request.json#/methods/TopologicalSort."""

    model_config = ConfigDict(extra="forbid")

    # Method::TopologicalSort carries no request fields.


async def send_topological_sort(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """TopologicalSort - compute:graph-algo, None, replay NotReplayable.

    Result: typed ResultPayload::Ids (contract/schemas/result.Ids.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TopologicalSortRequest.model_validate(params or {})
    payload = await client._send(
        "TopologicalSort",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_ids("TopologicalSort", payload)


class FindCycleRequest(BaseModel):
    """Request body for FindCycle - contract/schemas/method.request.json#/methods/FindCycle."""

    model_config = ConfigDict(extra="forbid")

    # Method::FindCycle carries no request fields.


async def send_find_cycle(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FindCycle - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FindCycleRequest.model_validate(params or {})
    payload = await client._send(
        "FindCycle",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FindCycle", payload)


class GetShortestPathRequest(BaseModel):
    """Request body for GetShortestPath - contract/schemas/method.request.json#/methods/GetShortestPath."""

    model_config = ConfigDict(extra="forbid")

    source_id: str
    target_id: str


async def send_get_shortest_path(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetShortestPath - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetShortestPathRequest.model_validate(params or {})
    payload = await client._send(
        "GetShortestPath",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetShortestPath", payload)


class GetBlastRadiusRequest(BaseModel):
    """Request body for GetBlastRadius - contract/schemas/method.request.json#/methods/GetBlastRadius."""

    model_config = ConfigDict(extra="forbid")

    max_depth: int
    node_id: str


async def send_get_blast_radius(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetBlastRadius - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Conflicting) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetBlastRadiusRequest.model_validate(params or {})
    payload = await client._send(
        "GetBlastRadius",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetBlastRadius", payload)


class DegreeCentralityRequest(BaseModel):
    """Request body for DegreeCentrality - contract/schemas/method.request.json#/methods/DegreeCentrality."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_degree_centrality(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """DegreeCentrality - compute:graph-algo, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DegreeCentralityRequest.model_validate(params or {})
    payload = await client._send(
        "DegreeCentrality",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("DegreeCentrality", payload)


class DegreeCentralityAllRequest(BaseModel):
    """Request body for DegreeCentralityAll - contract/schemas/method.request.json#/methods/DegreeCentralityAll."""

    model_config = ConfigDict(extra="forbid")

    # Method::DegreeCentralityAll carries no request fields.


async def send_degree_centrality_all(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DegreeCentralityAll - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DegreeCentralityAllRequest.model_validate(params or {})
    payload = await client._send(
        "DegreeCentralityAll",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DegreeCentralityAll", payload)


class BetweennessCentralityRequest(BaseModel):
    """Request body for BetweennessCentrality - contract/schemas/method.request.json#/methods/BetweennessCentrality."""

    model_config = ConfigDict(extra="forbid")

    # Method::BetweennessCentrality carries no request fields.


async def send_betweenness_centrality(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BetweennessCentrality - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    BetweennessCentralityRequest.model_validate(params or {})
    payload = await client._send(
        "BetweennessCentrality",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BetweennessCentrality", payload)


class PageRankRequest(BaseModel):
    """Request body for PageRank - contract/schemas/method.request.json#/methods/PageRank."""

    model_config = ConfigDict(extra="forbid")

    damping: float
    iterations: int


async def send_page_rank(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PageRank - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    PageRankRequest.model_validate(params or {})
    payload = await client._send(
        "PageRank",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PageRank", payload)


class PersonalizedPageRankRequest(BaseModel):
    """Request body for PersonalizedPageRank - contract/schemas/method.request.json#/methods/PersonalizedPageRank."""

    model_config = ConfigDict(extra="forbid")

    damping: float
    iterations: int
    seed_nodes: list[list[Any]]


async def send_personalized_page_rank(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PersonalizedPageRank - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    PersonalizedPageRankRequest.model_validate(params or {})
    payload = await client._send(
        "PersonalizedPageRank",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PersonalizedPageRank", payload)


class ConnectedComponentsRequest(BaseModel):
    """Request body for ConnectedComponents - contract/schemas/method.request.json#/methods/ConnectedComponents."""

    model_config = ConfigDict(extra="forbid")

    # Method::ConnectedComponents carries no request fields.


async def send_connected_components(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ConnectedComponents - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ConnectedComponentsRequest.model_validate(params or {})
    payload = await client._send(
        "ConnectedComponents",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ConnectedComponents", payload)


class StronglyConnectedComponentsRequest(BaseModel):
    """Request body for StronglyConnectedComponents - contract/schemas/method.request.json#/methods/StronglyConnectedComponents."""

    model_config = ConfigDict(extra="forbid")

    # Method::StronglyConnectedComponents carries no request fields.


async def send_strongly_connected_components(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """StronglyConnectedComponents - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    StronglyConnectedComponentsRequest.model_validate(params or {})
    payload = await client._send(
        "StronglyConnectedComponents",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("StronglyConnectedComponents", payload)


class MinimumSpanningTreeRequest(BaseModel):
    """Request body for MinimumSpanningTree - contract/schemas/method.request.json#/methods/MinimumSpanningTree."""

    model_config = ConfigDict(extra="forbid")

    # Method::MinimumSpanningTree carries no request fields.


async def send_minimum_spanning_tree(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MinimumSpanningTree - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    MinimumSpanningTreeRequest.model_validate(params or {})
    payload = await client._send(
        "MinimumSpanningTree",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MinimumSpanningTree", payload)


class CommunityDetectionRequest(BaseModel):
    """Request body for CommunityDetection - contract/schemas/method.request.json#/methods/CommunityDetection."""

    model_config = ConfigDict(extra="forbid")

    resolution: float


async def send_community_detection(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CommunityDetection - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CommunityDetectionRequest.model_validate(params or {})
    payload = await client._send(
        "CommunityDetection",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CommunityDetection", payload)


class CommunityDetectEphemeralRequest(BaseModel):
    """Request body for CommunityDetectEphemeral - contract/schemas/method.request.json#/methods/CommunityDetectEphemeral."""

    model_config = ConfigDict(extra="forbid")

    edges: list[list[Any]]
    node_ids: list[str]
    resolution: float


async def send_community_detect_ephemeral(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CommunityDetectEphemeral - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CommunityDetectEphemeralRequest.model_validate(params or {})
    payload = await client._send(
        "CommunityDetectEphemeral",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CommunityDetectEphemeral", payload)


class GraphColoringRequest(BaseModel):
    """Request body for GraphColoring - contract/schemas/method.request.json#/methods/GraphColoring."""

    model_config = ConfigDict(extra="forbid")

    # Method::GraphColoring carries no request fields.


async def send_graph_coloring(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GraphColoring - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GraphColoringRequest.model_validate(params or {})
    payload = await client._send(
        "GraphColoring",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GraphColoring", payload)


class ComputeSimilarityEdgesRequest(BaseModel):
    """Request body for ComputeSimilarityEdges - contract/schemas/method.request.json#/methods/ComputeSimilarityEdges."""

    model_config = ConfigDict(extra="forbid")

    threshold: float


async def send_compute_similarity_edges(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """ComputeSimilarityEdges - compute:graph-algo, None, replay NotReplayable.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ComputeSimilarityEdgesRequest.model_validate(params or {})
    payload = await client._send(
        "ComputeSimilarityEdges",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_count("ComputeSimilarityEdges", payload)


class ResolveCandidatesRequest(BaseModel):
    """Request body for ResolveCandidates - contract/schemas/method.request.json#/methods/ResolveCandidates."""

    model_config = ConfigDict(extra="forbid")

    merge_threshold: float
    node_type: Any | None = None
    sim_threshold: float


async def send_resolve_candidates(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ResolveCandidates - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ResolveCandidatesRequest.model_validate(params or {})
    payload = await client._send(
        "ResolveCandidates",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ResolveCandidates", payload)


class ClusterHierarchyRefreshRequest(BaseModel):
    """Request body for ClusterHierarchyRefresh - contract/schemas/method.request.json#/methods/ClusterHierarchyRefresh."""

    model_config = ConfigDict(extra="forbid")

    label: Any | None = None
    resolution: float | None = None
    seed: int | None = None


async def send_cluster_hierarchy_refresh(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ClusterHierarchyRefresh - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ClusterHierarchyRefreshRequest.model_validate(params or {})
    payload = await client._send(
        "ClusterHierarchyRefresh",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ClusterHierarchyRefresh", payload)


class ClusterHierarchyClustersRequest(BaseModel):
    """Request body for ClusterHierarchyClusters - contract/schemas/method.request.json#/methods/ClusterHierarchyClusters."""

    model_config = ConfigDict(extra="forbid")

    level: int
    parent_cluster_id: Any | None = None


async def send_cluster_hierarchy_clusters(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ClusterHierarchyClusters - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ClusterHierarchyClustersRequest.model_validate(params or {})
    payload = await client._send(
        "ClusterHierarchyClusters",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ClusterHierarchyClusters", payload)


class ClusterHierarchyExpandRequest(BaseModel):
    """Request body for ClusterHierarchyExpand - contract/schemas/method.request.json#/methods/ClusterHierarchyExpand."""

    model_config = ConfigDict(extra="forbid")

    cluster_id: str


async def send_cluster_hierarchy_expand(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ClusterHierarchyExpand - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ClusterHierarchyExpandRequest.model_validate(params or {})
    payload = await client._send(
        "ClusterHierarchyExpand",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ClusterHierarchyExpand", payload)


class Vf2SubgraphMatchRequest(BaseModel):
    """Request body for Vf2SubgraphMatch - contract/schemas/method.request.json#/methods/Vf2SubgraphMatch."""

    model_config = ConfigDict(extra="forbid")

    max_results: int | None = None
    max_steps: int | None = None
    pattern_graph_name: str


async def send_vf2_subgraph_match(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Vf2SubgraphMatch - compute:graph-algo, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    Vf2SubgraphMatchRequest.model_validate(params or {})
    payload = await client._send(
        "Vf2SubgraphMatch",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Vf2SubgraphMatch", payload)


class MatchOntologyTermsRequest(BaseModel):
    """Request body for MatchOntologyTerms - contract/schemas/method.request.json#/methods/MatchOntologyTerms."""

    model_config = ConfigDict(extra="forbid")

    query: str


async def send_match_ontology_terms(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MatchOntologyTerms - compute:semantic, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    MatchOntologyTermsRequest.model_validate(params or {})
    payload = await client._send(
        "MatchOntologyTerms",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MatchOntologyTerms", payload)


class BatchL2NormalizeRequest(BaseModel):
    """Request body for BatchL2Normalize - contract/schemas/method.request.json#/methods/BatchL2Normalize."""

    model_config = ConfigDict(extra="forbid")

    vectors: list[list[float]]


async def send_batch_l2_normalize(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BatchL2Normalize - compute:semantic, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    BatchL2NormalizeRequest.model_validate(params or {})
    payload = await client._send(
        "BatchL2Normalize",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BatchL2Normalize", payload)


class FinanceOptimizePortfolioRequest(BaseModel):
    """Request body for FinanceOptimizePortfolio - contract/schemas/method.request.json#/methods/FinanceOptimizePortfolio."""

    model_config = ConfigDict(extra="forbid")

    cov_matrix: list[list[float]]
    expected_returns: list[float]
    max_weight: Any | None = None
    min_weight: Any | None = None
    risk_free_rate: float


async def send_finance_optimize_portfolio(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceOptimizePortfolio - compute:finance, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceOptimizePortfolioRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceOptimizePortfolio",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceOptimizePortfolio", payload)


class FinanceRiskParityRequest(BaseModel):
    """Request body for FinanceRiskParity - contract/schemas/method.request.json#/methods/FinanceRiskParity."""

    model_config = ConfigDict(extra="forbid")

    cov_matrix: list[list[float]]


async def send_finance_risk_parity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceRiskParity - compute:finance, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceRiskParityRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceRiskParity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceRiskParity", payload)


class FinanceBlackLittermanRequest(BaseModel):
    """Request body for FinanceBlackLitterman - contract/schemas/method.request.json#/methods/FinanceBlackLitterman."""

    model_config = ConfigDict(extra="forbid")

    cov_matrix: list[list[float]]
    market_weights: list[float]
    pick_matrix: list[list[float]]
    risk_aversion: float
    tau: float
    views: list[float]


async def send_finance_black_litterman(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceBlackLitterman - compute:finance, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceBlackLittermanRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceBlackLitterman",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceBlackLitterman", payload)


class FinanceEfficientFrontierRequest(BaseModel):
    """Request body for FinanceEfficientFrontier - contract/schemas/method.request.json#/methods/FinanceEfficientFrontier."""

    model_config = ConfigDict(extra="forbid")

    cov_matrix: list[list[float]]
    expected_returns: list[float]
    target_return: float


async def send_finance_efficient_frontier(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceEfficientFrontier - compute:finance, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceEfficientFrontierRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceEfficientFrontier",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceEfficientFrontier", payload)


class DsLinearRegressionRequest(BaseModel):
    """Request body for DsLinearRegression - contract/schemas/method.request.json#/methods/DsLinearRegression."""

    model_config = ConfigDict(extra="forbid")

    x: list[list[float]]
    y: list[float]


async def send_ds_linear_regression(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsLinearRegression - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsLinearRegressionRequest.model_validate(params or {})
    payload = await client._send(
        "DsLinearRegression",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsLinearRegression", payload)


class DsKMeansRequest(BaseModel):
    """Request body for DsKMeans - contract/schemas/method.request.json#/methods/DsKMeans."""

    model_config = ConfigDict(extra="forbid")

    data: list[list[float]]
    k: int
    max_iter: int


async def send_ds_k_means(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsKMeans - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsKMeansRequest.model_validate(params or {})
    payload = await client._send(
        "DsKMeans",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsKMeans", payload)


class DsPcaRequest(BaseModel):
    """Request body for DsPca - contract/schemas/method.request.json#/methods/DsPca."""

    model_config = ConfigDict(extra="forbid")

    data: list[list[float]]
    n_components: int


async def send_ds_pca(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsPca - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsPcaRequest.model_validate(params or {})
    payload = await client._send(
        "DsPca",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsPca", payload)


class DsComputeStatsRequest(BaseModel):
    """Request body for DsComputeStats - contract/schemas/method.request.json#/methods/DsComputeStats."""

    model_config = ConfigDict(extra="forbid")

    data: list[list[float]]


async def send_ds_compute_stats(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsComputeStats - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsComputeStatsRequest.model_validate(params or {})
    payload = await client._send(
        "DsComputeStats",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsComputeStats", payload)


class DsTrainTestSplitRequest(BaseModel):
    """Request body for DsTrainTestSplit - contract/schemas/method.request.json#/methods/DsTrainTestSplit."""

    model_config = ConfigDict(extra="forbid")

    data: list[list[float]]
    labels: list[float]
    seed: int
    shuffle: bool
    test_ratio: float


async def send_ds_train_test_split(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsTrainTestSplit - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsTrainTestSplitRequest.model_validate(params or {})
    payload = await client._send(
        "DsTrainTestSplit",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsTrainTestSplit", payload)


class DsFitEstimatorRequest(BaseModel):
    """Request body for DsFitEstimator - contract/schemas/method.request.json#/methods/DsFitEstimator."""

    model_config = ConfigDict(extra="forbid")

    estimator: str
    params: Any | None = None
    x: list[list[float]]
    y: list[float]


async def send_ds_fit_estimator(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsFitEstimator - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsFitEstimatorRequest.model_validate(params or {})
    payload = await client._send(
        "DsFitEstimator",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsFitEstimator", payload)


class DsPredictEstimatorRequest(BaseModel):
    """Request body for DsPredictEstimator - contract/schemas/method.request.json#/methods/DsPredictEstimator."""

    model_config = ConfigDict(extra="forbid")

    model: Any
    x: list[list[float]]


async def send_ds_predict_estimator(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsPredictEstimator - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsPredictEstimatorRequest.model_validate(params or {})
    payload = await client._send(
        "DsPredictEstimator",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsPredictEstimator", payload)


class DsSoftmaxRequest(BaseModel):
    """Request body for DsSoftmax - contract/schemas/method.request.json#/methods/DsSoftmax."""

    model_config = ConfigDict(extra="forbid")

    logits: list[float]
    temperature: float


async def send_ds_softmax(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsSoftmax - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsSoftmaxRequest.model_validate(params or {})
    payload = await client._send(
        "DsSoftmax",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsSoftmax", payload)


class DsLogSoftmaxRequest(BaseModel):
    """Request body for DsLogSoftmax - contract/schemas/method.request.json#/methods/DsLogSoftmax."""

    model_config = ConfigDict(extra="forbid")

    logits: list[float]


async def send_ds_log_softmax(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsLogSoftmax - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsLogSoftmaxRequest.model_validate(params or {})
    payload = await client._send(
        "DsLogSoftmax",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsLogSoftmax", payload)


class DsCrossEntropyRequest(BaseModel):
    """Request body for DsCrossEntropy - contract/schemas/method.request.json#/methods/DsCrossEntropy."""

    model_config = ConfigDict(extra="forbid")

    labels: list[int]
    logits: list[list[float]]


async def send_ds_cross_entropy(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsCrossEntropy - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsCrossEntropyRequest.model_validate(params or {})
    payload = await client._send(
        "DsCrossEntropy",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsCrossEntropy", payload)


class DsDpoLossRequest(BaseModel):
    """Request body for DsDpoLoss - contract/schemas/method.request.json#/methods/DsDpoLoss."""

    model_config = ConfigDict(extra="forbid")

    beta: float
    policy_chosen: list[float]
    policy_rejected: list[float]
    ref_chosen: list[float]
    ref_rejected: list[float]


async def send_ds_dpo_loss(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsDpoLoss - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsDpoLossRequest.model_validate(params or {})
    payload = await client._send(
        "DsDpoLoss",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsDpoLoss", payload)


class DsGrpoSurrogateRequest(BaseModel):
    """Request body for DsGrpoSurrogate - contract/schemas/method.request.json#/methods/DsGrpoSurrogate."""

    model_config = ConfigDict(extra="forbid")

    advantage: list[float]
    clip_eps: float
    logprob: list[float]
    old_logprob: list[float]


async def send_ds_grpo_surrogate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsGrpoSurrogate - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsGrpoSurrogateRequest.model_validate(params or {})
    payload = await client._send(
        "DsGrpoSurrogate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsGrpoSurrogate", payload)


class DsKlDivergenceRequest(BaseModel):
    """Request body for DsKlDivergence - contract/schemas/method.request.json#/methods/DsKlDivergence."""

    model_config = ConfigDict(extra="forbid")

    logprob: list[float]
    ref_logprob: list[float]


async def send_ds_kl_divergence(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """DsKlDivergence - compute:datascience, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsKlDivergenceRequest.model_validate(params or {})
    payload = await client._send(
        "DsKlDivergence",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("DsKlDivergence", payload)


class DsAdamStepRequest(BaseModel):
    """Request body for DsAdamStep - contract/schemas/method.request.json#/methods/DsAdamStep."""

    model_config = ConfigDict(extra="forbid")

    beta1: float
    beta2: float
    eps: float
    grads: list[float]
    lr: float
    m: list[float]
    params: list[float]
    t: int
    v: list[float]


async def send_ds_adam_step(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsAdamStep - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsAdamStepRequest.model_validate(params or {})
    payload = await client._send(
        "DsAdamStep",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsAdamStep", payload)


class DsSgdStepRequest(BaseModel):
    """Request body for DsSgdStep - contract/schemas/method.request.json#/methods/DsSgdStep."""

    model_config = ConfigDict(extra="forbid")

    grads: list[float]
    lr: float
    params: list[float]


async def send_ds_sgd_step(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DsSgdStep - compute:datascience, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DsSgdStepRequest.model_validate(params or {})
    payload = await client._send(
        "DsSgdStep",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DsSgdStep", payload)


class FinanceVarRequest(BaseModel):
    """Request body for FinanceVar - contract/schemas/method.request.json#/methods/FinanceVar."""

    model_config = ConfigDict(extra="forbid")

    confidence: float
    returns: list[float]


async def send_finance_var(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceVar - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceVarRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceVar",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceVar", payload)


class FinanceCvarRequest(BaseModel):
    """Request body for FinanceCvar - contract/schemas/method.request.json#/methods/FinanceCvar."""

    model_config = ConfigDict(extra="forbid")

    confidence: float
    returns: list[float]


async def send_finance_cvar(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceCvar - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceCvarRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceCvar",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceCvar", payload)


class FinanceMaxDrawdownRequest(BaseModel):
    """Request body for FinanceMaxDrawdown - contract/schemas/method.request.json#/methods/FinanceMaxDrawdown."""

    model_config = ConfigDict(extra="forbid")

    returns: list[float]


async def send_finance_max_drawdown(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceMaxDrawdown - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMaxDrawdownRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMaxDrawdown",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceMaxDrawdown", payload)


class FinanceDrawdownSeriesRequest(BaseModel):
    """Request body for FinanceDrawdownSeries - contract/schemas/method.request.json#/methods/FinanceDrawdownSeries."""

    model_config = ConfigDict(extra="forbid")

    returns: list[float]


async def send_finance_drawdown_series(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceDrawdownSeries - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceDrawdownSeriesRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceDrawdownSeries",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceDrawdownSeries", payload)


class FinanceDownsideDeviationRequest(BaseModel):
    """Request body for FinanceDownsideDeviation - contract/schemas/method.request.json#/methods/FinanceDownsideDeviation."""

    model_config = ConfigDict(extra="forbid")

    returns: list[float]
    target: float


async def send_finance_downside_deviation(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceDownsideDeviation - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceDownsideDeviationRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceDownsideDeviation",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceDownsideDeviation", payload)


class FinanceRiskMetricsRequest(BaseModel):
    """Request body for FinanceRiskMetrics - contract/schemas/method.request.json#/methods/FinanceRiskMetrics."""

    model_config = ConfigDict(extra="forbid")

    returns: list[float]
    risk_free_rate: float


async def send_finance_risk_metrics(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceRiskMetrics - compute:finance, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceRiskMetricsRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceRiskMetrics",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceRiskMetrics", payload)


class FinanceMonteCarloVarRequest(BaseModel):
    """Request body for FinanceMonteCarloVar - contract/schemas/method.request.json#/methods/FinanceMonteCarloVar."""

    model_config = ConfigDict(extra="forbid")

    confidence: float
    mean: float
    n_simulations: int
    std_dev: float


async def send_finance_monte_carlo_var(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceMonteCarloVar - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMonteCarloVarRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMonteCarloVar",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceMonteCarloVar", payload)


class FinanceStressTestRequest(BaseModel):
    """Request body for FinanceStressTest - contract/schemas/method.request.json#/methods/FinanceStressTest."""

    model_config = ConfigDict(extra="forbid")

    cov_matrix: list[list[float]]
    expected_returns: list[float]
    shock_factors: list[float]
    weights: list[float]


async def send_finance_stress_test(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceStressTest - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceStressTestRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceStressTest",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceStressTest", payload)


class FinanceDetectRegimesRequest(BaseModel):
    """Request body for FinanceDetectRegimes - contract/schemas/method.request.json#/methods/FinanceDetectRegimes."""

    model_config = ConfigDict(extra="forbid")

    max_iter: int
    n_states: int
    observations: list[float]
    tol: float


async def send_finance_detect_regimes(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceDetectRegimes - compute:finance, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceDetectRegimesRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceDetectRegimes",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceDetectRegimes", payload)


class FinanceRollingZscoreRequest(BaseModel):
    """Request body for FinanceRollingZscore - contract/schemas/method.request.json#/methods/FinanceRollingZscore."""

    model_config = ConfigDict(extra="forbid")

    values: list[float]
    window: int


async def send_finance_rolling_zscore(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceRollingZscore - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceRollingZscoreRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceRollingZscore",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceRollingZscore", payload)


class FinanceEwmaRequest(BaseModel):
    """Request body for FinanceEwma - contract/schemas/method.request.json#/methods/FinanceEwma."""

    model_config = ConfigDict(extra="forbid")

    span: int
    values: list[float]


async def send_finance_ewma(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceEwma - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceEwmaRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceEwma",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceEwma", payload)


class FinanceSignalDecayRequest(BaseModel):
    """Request body for FinanceSignalDecay - contract/schemas/method.request.json#/methods/FinanceSignalDecay."""

    model_config = ConfigDict(extra="forbid")

    half_life: float
    signal: list[float]


async def send_finance_signal_decay(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceSignalDecay - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceSignalDecayRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceSignalDecay",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceSignalDecay", payload)


class FinanceCombineAlphasRequest(BaseModel):
    """Request body for FinanceCombineAlphas - contract/schemas/method.request.json#/methods/FinanceCombineAlphas."""

    model_config = ConfigDict(extra="forbid")

    signals: list[list[float]]
    weights: list[float]


async def send_finance_combine_alphas(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceCombineAlphas - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceCombineAlphasRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceCombineAlphas",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceCombineAlphas", payload)


class FinanceCrossSectionalRankRequest(BaseModel):
    """Request body for FinanceCrossSectionalRank - contract/schemas/method.request.json#/methods/FinanceCrossSectionalRank."""

    model_config = ConfigDict(extra="forbid")

    cross_section: list[list[float]]


async def send_finance_cross_sectional_rank(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceCrossSectionalRank - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceCrossSectionalRankRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceCrossSectionalRank",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceCrossSectionalRank", payload)


class FinanceMomentumRequest(BaseModel):
    """Request body for FinanceMomentum - contract/schemas/method.request.json#/methods/FinanceMomentum."""

    model_config = ConfigDict(extra="forbid")

    lookback: int
    prices: list[float]


async def send_finance_momentum(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceMomentum - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMomentumRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMomentum",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceMomentum", payload)


class FinanceMeanReversionRequest(BaseModel):
    """Request body for FinanceMeanReversion - contract/schemas/method.request.json#/methods/FinanceMeanReversion."""

    model_config = ConfigDict(extra="forbid")

    values: list[float]
    window: int


async def send_finance_mean_reversion(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceMeanReversion - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMeanReversionRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMeanReversion",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceMeanReversion", payload)


class FinanceInformationCoefficientRequest(BaseModel):
    """Request body for FinanceInformationCoefficient - contract/schemas/method.request.json#/methods/FinanceInformationCoefficient."""

    model_config = ConfigDict(extra="forbid")

    forward_returns: list[float]
    signal: list[float]


async def send_finance_information_coefficient(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceInformationCoefficient - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceInformationCoefficientRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceInformationCoefficient",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceInformationCoefficient", payload)


class FinanceTwapRequest(BaseModel):
    """Request body for FinanceTwap - contract/schemas/method.request.json#/methods/FinanceTwap."""

    model_config = ConfigDict(extra="forbid")

    interval_secs: int
    n_slices: int
    start_time: int
    total_quantity: float


async def send_finance_twap(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceTwap - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceTwapRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceTwap",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceTwap", payload)


class FinanceVwapRequest(BaseModel):
    """Request body for FinanceVwap - contract/schemas/method.request.json#/methods/FinanceVwap."""

    model_config = ConfigDict(extra="forbid")

    interval_secs: int
    start_time: int
    total_quantity: float
    volume_profile: list[float]


async def send_finance_vwap(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceVwap - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceVwapRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceVwap",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceVwap", payload)


class FinanceMarketImpactRequest(BaseModel):
    """Request body for FinanceMarketImpact - contract/schemas/method.request.json#/methods/FinanceMarketImpact."""

    model_config = ConfigDict(extra="forbid")

    average_daily_volume: float
    daily_volatility: float
    impact_coefficient: float
    order_quantity: float


async def send_finance_market_impact(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceMarketImpact - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMarketImpactRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMarketImpact",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceMarketImpact", payload)


class FinancePairsTradingRequest(BaseModel):
    """Request body for FinancePairsTrading - contract/schemas/method.request.json#/methods/FinancePairsTrading."""

    model_config = ConfigDict(extra="forbid")

    lookback: int
    prices_a: list[float]
    prices_b: list[float]


async def send_finance_pairs_trading(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinancePairsTrading - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinancePairsTradingRequest.model_validate(params or {})
    payload = await client._send(
        "FinancePairsTrading",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinancePairsTrading", payload)


class FinanceMatchOrdersRequest(BaseModel):
    """Request body for FinanceMatchOrders - contract/schemas/method.request.json#/methods/FinanceMatchOrders."""

    model_config = ConfigDict(extra="forbid")

    orders: list[Any]


async def send_finance_match_orders(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceMatchOrders - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMatchOrdersRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMatchOrders",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceMatchOrders", payload)


class FinanceAvellanedaStoikovRequest(BaseModel):
    """Request body for FinanceAvellanedaStoikov - contract/schemas/method.request.json#/methods/FinanceAvellanedaStoikov."""

    model_config = ConfigDict(extra="forbid")

    gamma: float
    inventory: float
    kappa: float
    mid: float
    sigma: float
    tau: float


async def send_finance_avellaneda_stoikov(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceAvellanedaStoikov - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceAvellanedaStoikovRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceAvellanedaStoikov",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceAvellanedaStoikov", payload)


class FinanceGltQuotesRequest(BaseModel):
    """Request body for FinanceGltQuotes - contract/schemas/method.request.json#/methods/FinanceGltQuotes."""

    model_config = ConfigDict(extra="forbid")

    a: float
    gamma: float
    inventory: float
    kappa: float
    mid: float
    sigma: float


async def send_finance_glt_quotes(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceGltQuotes - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceGltQuotesRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceGltQuotes",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceGltQuotes", payload)


class FinanceLogitQuotesRequest(BaseModel):
    """Request body for FinanceLogitQuotes - contract/schemas/method.request.json#/methods/FinanceLogitQuotes."""

    model_config = ConfigDict(extra="forbid")

    boundary_m: float
    gamma: float
    inventory: float
    kappa: float
    p_mid: float
    sigma: float
    tau: float


async def send_finance_logit_quotes(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceLogitQuotes - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceLogitQuotesRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceLogitQuotes",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceLogitQuotes", payload)


class FinanceGlostenMilgromSpreadRequest(BaseModel):
    """Request body for FinanceGlostenMilgromSpread - contract/schemas/method.request.json#/methods/FinanceGlostenMilgromSpread."""

    model_config = ConfigDict(extra="forbid")

    alpha: float
    p: float


async def send_finance_glosten_milgrom_spread(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceGlostenMilgromSpread - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceGlostenMilgromSpreadRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceGlostenMilgromSpread",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceGlostenMilgromSpread", payload)


class FinanceExpectedPnlRateRequest(BaseModel):
    """Request body for FinanceExpectedPnlRate - contract/schemas/method.request.json#/methods/FinanceExpectedPnlRate."""

    model_config = ConfigDict(extra="forbid")

    a: float
    alpha: float
    delta: float
    kappa: float
    p: float
    v_h: float
    v_l: float


async def send_finance_expected_pnl_rate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceExpectedPnlRate - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceExpectedPnlRateRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceExpectedPnlRate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceExpectedPnlRate", payload)


class FinanceBreakevenAlphaRequest(BaseModel):
    """Request body for FinanceBreakevenAlpha - contract/schemas/method.request.json#/methods/FinanceBreakevenAlpha."""

    model_config = ConfigDict(extra="forbid")

    delta: float
    p: float
    v_h: float
    v_l: float


async def send_finance_breakeven_alpha(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceBreakevenAlpha - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceBreakevenAlphaRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceBreakevenAlpha",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceBreakevenAlpha", payload)


class FinanceOfiSeriesRequest(BaseModel):
    """Request body for FinanceOfiSeries - contract/schemas/method.request.json#/methods/FinanceOfiSeries."""

    model_config = ConfigDict(extra="forbid")

    ask_px: list[float]
    ask_sz: list[float]
    bid_px: list[float]
    bid_sz: list[float]
    ts: list[float]
    window_secs: float


async def send_finance_ofi_series(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceOfiSeries - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceOfiSeriesRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceOfiSeries",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceOfiSeries", payload)


class FinanceMicropriceSeriesRequest(BaseModel):
    """Request body for FinanceMicropriceSeries - contract/schemas/method.request.json#/methods/FinanceMicropriceSeries."""

    model_config = ConfigDict(extra="forbid")

    ask_px: list[float]
    ask_sz: list[float]
    bid_px: list[float]
    bid_sz: list[float]


async def send_finance_microprice_series(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceMicropriceSeries - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMicropriceSeriesRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMicropriceSeries",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceMicropriceSeries", payload)


class FinanceVpinPmRequest(BaseModel):
    """Request body for FinanceVpinPm - contract/schemas/method.request.json#/methods/FinanceVpinPm."""

    model_config = ConfigDict(extra="forbid")

    buy_vol: list[float]
    p_mean: list[float]
    sell_vol: list[float]


async def send_finance_vpin_pm(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceVpinPm - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceVpinPmRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceVpinPm",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceVpinPm", payload)


class FinanceHawkesMleRequest(BaseModel):
    """Request body for FinanceHawkesMle - contract/schemas/method.request.json#/methods/FinanceHawkesMle."""

    model_config = ConfigDict(extra="forbid")

    max_iter: int
    t_horizon: float
    times: list[float]


async def send_finance_hawkes_mle(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceHawkesMle - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceHawkesMleRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceHawkesMle",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceHawkesMle", payload)


class FinanceHardimanBouchaudRequest(BaseModel):
    """Request body for FinanceHardimanBouchaud - contract/schemas/method.request.json#/methods/FinanceHardimanBouchaud."""

    model_config = ConfigDict(extra="forbid")

    n_windows: int
    t_horizon: float
    times: list[float]


async def send_finance_hardiman_bouchaud(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceHardimanBouchaud - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceHardimanBouchaudRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceHardimanBouchaud",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceHardimanBouchaud", payload)


class FinanceKyleLambdaRequest(BaseModel):
    """Request body for FinanceKyleLambda - contract/schemas/method.request.json#/methods/FinanceKyleLambda."""

    model_config = ConfigDict(extra="forbid")

    price_changes: list[float]
    signed_order_flow: list[float]


async def send_finance_kyle_lambda(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceKyleLambda - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceKyleLambdaRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceKyleLambda",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceKyleLambda", payload)


class FinanceSurveillanceRiskRequest(BaseModel):
    """Request body for FinanceSurveillanceRisk - contract/schemas/method.request.json#/methods/FinanceSurveillanceRisk."""

    model_config = ConfigDict(extra="forbid")

    baseline_sigma: float
    buy_vol: list[float]
    p_mean: list[float]
    price_changes: list[float]
    sell_vol: list[float]
    signed_flow: list[float]


async def send_finance_surveillance_risk(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceSurveillanceRisk - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceSurveillanceRiskRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceSurveillanceRisk",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceSurveillanceRisk", payload)


class FinanceKellyFractionRequest(BaseModel):
    """Request body for FinanceKellyFraction - contract/schemas/method.request.json#/methods/FinanceKellyFraction."""

    model_config = ConfigDict(extra="forbid")

    c: float
    fraction: float
    q: float


async def send_finance_kelly_fraction(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceKellyFraction - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceKellyFractionRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceKellyFraction",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceKellyFraction", payload)


class FinanceBayesianKellyRequest(BaseModel):
    """Request body for FinanceBayesianKelly - contract/schemas/method.request.json#/methods/FinanceBayesianKelly."""

    model_config = ConfigDict(extra="forbid")

    alpha: float
    beta: float
    c: float
    n_quadrature: int


async def send_finance_bayesian_kelly(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceBayesianKelly - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceBayesianKellyRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceBayesianKelly",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceBayesianKelly", payload)


class FinancePosteriorCredibleIntervalRequest(BaseModel):
    """Request body for FinancePosteriorCredibleInterval - contract/schemas/method.request.json#/methods/FinancePosteriorCredibleInterval."""

    model_config = ConfigDict(extra="forbid")

    alpha: float
    beta: float
    level: float


async def send_finance_posterior_credible_interval(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinancePosteriorCredibleInterval - compute:finance, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinancePosteriorCredibleIntervalRequest.model_validate(params or {})
    payload = await client._send(
        "FinancePosteriorCredibleInterval",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinancePosteriorCredibleInterval", payload)


class FinancePurgedCpcvRequest(BaseModel):
    """Request body for FinancePurgedCpcv - contract/schemas/method.request.json#/methods/FinancePurgedCpcv."""

    model_config = ConfigDict(extra="forbid")

    embargo: int
    n_groups: int
    n_samples: int
    n_test_groups: int
    purge_window: int


async def send_finance_purged_cpcv(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinancePurgedCpcv - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinancePurgedCpcvRequest.model_validate(params or {})
    payload = await client._send(
        "FinancePurgedCpcv",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinancePurgedCpcv", payload)


class FinanceDeflatedSharpeRequest(BaseModel):
    """Request body for FinanceDeflatedSharpe - contract/schemas/method.request.json#/methods/FinanceDeflatedSharpe."""

    model_config = ConfigDict(extra="forbid")

    n_trials: int
    observed_sr: float
    sr_returns: list[float]


async def send_finance_deflated_sharpe(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceDeflatedSharpe - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceDeflatedSharpeRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceDeflatedSharpe",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceDeflatedSharpe", payload)


class FinanceProbabilityBacktestOverfitRequest(BaseModel):
    """Request body for FinanceProbabilityBacktestOverfit - contract/schemas/method.request.json#/methods/FinanceProbabilityBacktestOverfit."""

    model_config = ConfigDict(extra="forbid")

    insample: list[list[float]]
    oos: list[list[float]]


async def send_finance_probability_backtest_overfit(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceProbabilityBacktestOverfit - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceProbabilityBacktestOverfitRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceProbabilityBacktestOverfit",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceProbabilityBacktestOverfit", payload)


class FinanceDieboldMarianoRequest(BaseModel):
    """Request body for FinanceDieboldMariano - contract/schemas/method.request.json#/methods/FinanceDieboldMariano."""

    model_config = ConfigDict(extra="forbid")

    h: int
    losses_a: list[float]
    losses_b: list[float]


async def send_finance_diebold_mariano(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceDieboldMariano - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceDieboldMarianoRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceDieboldMariano",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceDieboldMariano", payload)


class FinanceForensicReportRequest(BaseModel):
    """Request body for FinanceForensicReport - contract/schemas/method.request.json#/methods/FinanceForensicReport."""

    model_config = ConfigDict(extra="forbid")

    prior_year: Any
    this_year: Any


async def send_finance_forensic_report(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceForensicReport - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceForensicReportRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceForensicReport",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceForensicReport", payload)


class FinanceKalmanFilter1dRequest(BaseModel):
    """Request body for FinanceKalmanFilter1d - contract/schemas/method.request.json#/methods/FinanceKalmanFilter1d."""

    model_config = ConfigDict(extra="forbid")

    f: float
    h: float
    observations: list[float]
    p0: float
    q: float
    r: float
    x0: float


async def send_finance_kalman_filter1d(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceKalmanFilter1d - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceKalmanFilter1dRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceKalmanFilter1d",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceKalmanFilter1d", payload)


class FinanceKalmanBetaRequest(BaseModel):
    """Request body for FinanceKalmanBeta - contract/schemas/method.request.json#/methods/FinanceKalmanBeta."""

    model_config = ConfigDict(extra="forbid")

    asset_returns: list[float]
    beta0: float
    market_returns: list[float]
    p0: float
    q: float
    r: float


async def send_finance_kalman_beta(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceKalmanBeta - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceKalmanBetaRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceKalmanBeta",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceKalmanBeta", payload)


class FinanceKalmanVolatilityRequest(BaseModel):
    """Request body for FinanceKalmanVolatility - contract/schemas/method.request.json#/methods/FinanceKalmanVolatility."""

    model_config = ConfigDict(extra="forbid")

    annualization: float
    log_var0: Any | None = None
    p0: float
    q: float
    r: float
    returns: list[float]


async def send_finance_kalman_volatility(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceKalmanVolatility - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceKalmanVolatilityRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceKalmanVolatility",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceKalmanVolatility", payload)


class FinanceAdfTestRequest(BaseModel):
    """Request body for FinanceAdfTest - contract/schemas/method.request.json#/methods/FinanceAdfTest."""

    model_config = ConfigDict(extra="forbid")

    max_lag: int
    series: list[float]


async def send_finance_adf_test(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceAdfTest - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceAdfTestRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceAdfTest",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceAdfTest", payload)


class FinanceOuCalibrateRequest(BaseModel):
    """Request body for FinanceOuCalibrate - contract/schemas/method.request.json#/methods/FinanceOuCalibrate."""

    model_config = ConfigDict(extra="forbid")

    dt: float
    spread: list[float]


async def send_finance_ou_calibrate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceOuCalibrate - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceOuCalibrateRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceOuCalibrate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceOuCalibrate", payload)


class FinanceOuOptimalThresholdsRequest(BaseModel):
    """Request body for FinanceOuOptimalThresholds - contract/schemas/method.request.json#/methods/FinanceOuOptimalThresholds."""

    model_config = ConfigDict(extra="forbid")

    cost: float
    mu: float
    sigma: float
    sigma_eq: float
    theta: float


async def send_finance_ou_optimal_thresholds(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceOuOptimalThresholds - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceOuOptimalThresholdsRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceOuOptimalThresholds",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceOuOptimalThresholds", payload)


class FinanceMarkovTransitionMatrixRequest(BaseModel):
    """Request body for FinanceMarkovTransitionMatrix - contract/schemas/method.request.json#/methods/FinanceMarkovTransitionMatrix."""

    model_config = ConfigDict(extra="forbid")

    n_states: int
    states: list[int]


async def send_finance_markov_transition_matrix(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceMarkovTransitionMatrix - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceMarkovTransitionMatrixRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceMarkovTransitionMatrix",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceMarkovTransitionMatrix", payload)


class FinanceOrderBookImbalanceRequest(BaseModel):
    """Request body for FinanceOrderBookImbalance - contract/schemas/method.request.json#/methods/FinanceOrderBookImbalance."""

    model_config = ConfigDict(extra="forbid")

    v_ask: list[float]
    v_bid: list[float]


async def send_finance_order_book_imbalance(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceOrderBookImbalance - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceOrderBookImbalanceRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceOrderBookImbalance",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceOrderBookImbalance", payload)


class FinanceQueueImbalanceRequest(BaseModel):
    """Request body for FinanceQueueImbalance - contract/schemas/method.request.json#/methods/FinanceQueueImbalance."""

    model_config = ConfigDict(extra="forbid")

    ask_q: list[float]
    ask_rate: list[float]
    bid_q: list[float]
    bid_rate: list[float]


async def send_finance_queue_imbalance(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceQueueImbalance - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceQueueImbalanceRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceQueueImbalance",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceQueueImbalance", payload)


class FinanceRealizedVolTickRequest(BaseModel):
    """Request body for FinanceRealizedVolTick - contract/schemas/method.request.json#/methods/FinanceRealizedVolTick."""

    model_config = ConfigDict(extra="forbid")

    mid: list[float]
    window: int


async def send_finance_realized_vol_tick(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceRealizedVolTick - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceRealizedVolTickRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceRealizedVolTick",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceRealizedVolTick", payload)


class FinanceSpreadReversionRequest(BaseModel):
    """Request body for FinanceSpreadReversion - contract/schemas/method.request.json#/methods/FinanceSpreadReversion."""

    model_config = ConfigDict(extra="forbid")

    ask_px: list[float]
    bid_px: list[float]
    window: int


async def send_finance_spread_reversion(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceSpreadReversion - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceSpreadReversionRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceSpreadReversion",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceSpreadReversion", payload)


class FinanceInformationRatioRequest(BaseModel):
    """Request body for FinanceInformationRatio - contract/schemas/method.request.json#/methods/FinanceInformationRatio."""

    model_config = ConfigDict(extra="forbid")

    ic: float
    n_independent: float


async def send_finance_information_ratio(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceInformationRatio - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceInformationRatioRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceInformationRatio",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceInformationRatio", payload)


class FinanceEffectiveIndependentNRequest(BaseModel):
    """Request body for FinanceEffectiveIndependentN - contract/schemas/method.request.json#/methods/FinanceEffectiveIndependentN."""

    model_config = ConfigDict(extra="forbid")

    returns_matrix: list[list[float]]


async def send_finance_effective_independent_n(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceEffectiveIndependentN - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceEffectiveIndependentNRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceEffectiveIndependentN",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceEffectiveIndependentN", payload)


class FinanceAlphaCombinationEngineRequest(BaseModel):
    """Request body for FinanceAlphaCombinationEngine - contract/schemas/method.request.json#/methods/FinanceAlphaCombinationEngine."""

    model_config = ConfigDict(extra="forbid")

    lookback: int
    returns_matrix: list[list[float]]


async def send_finance_alpha_combination_engine(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceAlphaCombinationEngine - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceAlphaCombinationEngineRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceAlphaCombinationEngine",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceAlphaCombinationEngine", payload)


class FinanceBrierScoreRequest(BaseModel):
    """Request body for FinanceBrierScore - contract/schemas/method.request.json#/methods/FinanceBrierScore."""

    model_config = ConfigDict(extra="forbid")

    forecasts: list[float]
    outcomes: list[float]


async def send_finance_brier_score(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceBrierScore - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceBrierScoreRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceBrierScore",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceBrierScore", payload)


class FinanceConvergenceGateRequest(BaseModel):
    """Request body for FinanceConvergenceGate - contract/schemas/method.request.json#/methods/FinanceConvergenceGate."""

    model_config = ConfigDict(extra="forbid")

    min_agree: int
    strengths: list[float]
    strong_threshold: float


async def send_finance_convergence_gate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceConvergenceGate - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceConvergenceGateRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceConvergenceGate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceConvergenceGate", payload)


class FinanceEmpiricalKellyRequest(BaseModel):
    """Request body for FinanceEmpiricalKelly - contract/schemas/method.request.json#/methods/FinanceEmpiricalKelly."""

    model_config = ConfigDict(extra="forbid")

    b: float
    historical_returns: list[float]
    n_simulations: int
    p: float
    seed: int


async def send_finance_empirical_kelly(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceEmpiricalKelly - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceEmpiricalKellyRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceEmpiricalKelly",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceEmpiricalKelly", payload)


class FinanceSabrImpliedVolRequest(BaseModel):
    """Request body for FinanceSabrImpliedVol - contract/schemas/method.request.json#/methods/FinanceSabrImpliedVol."""

    model_config = ConfigDict(extra="forbid")

    alpha: float
    beta: float
    f: float
    k: float
    nu: float
    rho: float
    t: float


async def send_finance_sabr_implied_vol(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> float:
    """FinanceSabrImpliedVol - compute:finance, None, replay NotReplayable.

    Result: typed ResultPayload::Float (contract/schemas/result.Float.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceSabrImpliedVolRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceSabrImpliedVol",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_float("FinanceSabrImpliedVol", payload)


class FinanceSabrSmileRequest(BaseModel):
    """Request body for FinanceSabrSmile - contract/schemas/method.request.json#/methods/FinanceSabrSmile."""

    model_config = ConfigDict(extra="forbid")

    alpha: float
    beta: float
    f: float
    nu: float
    rho: float
    strikes: list[float]
    t: float


async def send_finance_sabr_smile(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceSabrSmile - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceSabrSmileRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceSabrSmile",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceSabrSmile", payload)


class FinanceSabrCalibrateRequest(BaseModel):
    """Request body for FinanceSabrCalibrate - contract/schemas/method.request.json#/methods/FinanceSabrCalibrate."""

    model_config = ConfigDict(extra="forbid")

    beta: float
    f: float
    market_vols: list[float]
    strikes: list[float]
    t: float


async def send_finance_sabr_calibrate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinanceSabrCalibrate - compute:finance, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FinanceSabrCalibrateRequest.model_validate(params or {})
    payload = await client._send(
        "FinanceSabrCalibrate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinanceSabrCalibrate", payload)


class MineAssociateRequest(BaseModel):
    """Request body for MineAssociate - contract/schemas/method.request.json#/methods/MineAssociate."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    as_claim: bool | None = None
    min_confidence: float | None = None
    min_support: float | None = None
    source: Any | None = None
    transactions: list[list[str]] | None = None
    writeback: bool | None = None


async def send_mine_associate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineAssociate - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineAssociateRequest.model_validate(params or {})
    payload = await client._send(
        "MineAssociate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineAssociate", payload)


class MineClusterRequest(BaseModel):
    """Request body for MineCluster - contract/schemas/method.request.json#/methods/MineCluster."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    as_claim: bool | None = None
    eps: float | None = None
    features: list[list[float]] | None = None
    k: int | None = None
    linkage: Any | None = None
    max_iter: int | None = None
    min_pts: int | None = None
    plan: Any | None = None
    seed: int | None = None
    source: Any | None = None
    writeback: bool | None = None


async def send_mine_cluster(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineCluster - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineClusterRequest.model_validate(params or {})
    payload = await client._send(
        "MineCluster",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineCluster", payload)


class MineAnomalyRequest(BaseModel):
    """Request body for MineAnomaly - contract/schemas/method.request.json#/methods/MineAnomaly."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    as_claim: bool | None = None
    features: list[list[float]] | None = None
    gamma: float | None = None
    k: int | None = None
    kernel: Any | None = None
    n_trees: int | None = None
    nu: float | None = None
    plan: Any | None = None
    sample_size: int | None = None
    seed: int | None = None
    source: Any | None = None
    threshold: Any | None = None
    values: list[float] | None = None
    writeback: bool | None = None


async def send_mine_anomaly(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineAnomaly - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineAnomalyRequest.model_validate(params or {})
    payload = await client._send(
        "MineAnomaly",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineAnomaly", payload)


class MineClassifyFitRequest(BaseModel):
    """Request body for MineClassifyFit - contract/schemas/method.request.json#/methods/MineClassifyFit."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    alpha: float | None = None
    c: float | None = None
    epochs: int | None = None
    k: int | None = None
    l2: float | None = None
    lr: float | None = None
    plan: Any | None = None
    source: Any | None = None
    x: list[list[float]] | None = None
    y: list[int] | None = None


async def send_mine_classify_fit(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineClassifyFit - mining:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    MineClassifyFitRequest.model_validate(params or {})
    payload = await client._send(
        "MineClassifyFit",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineClassifyFit", payload)


class MineClassifyPredictRequest(BaseModel):
    """Request body for MineClassifyPredict - contract/schemas/method.request.json#/methods/MineClassifyPredict."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    model: Any
    plan: Any | None = None
    source: Any | None = None
    writeback: bool | None = None
    x: list[list[float]] | None = None


async def send_mine_classify_predict(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineClassifyPredict - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineClassifyPredictRequest.model_validate(params or {})
    payload = await client._send(
        "MineClassifyPredict",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineClassifyPredict", payload)


class MineReduceRequest(BaseModel):
    """Request body for MineReduce - contract/schemas/method.request.json#/methods/MineReduce."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    as_claim: bool | None = None
    epochs: int | None = None
    labels: list[int] | None = None
    lr: float | None = None
    min_dist: float | None = None
    n_components: int | None = None
    n_neighbors: int | None = None
    perplexity: float | None = None
    plan: Any | None = None
    seed: int | None = None
    source: Any | None = None
    writeback: bool | None = None
    x: list[list[float]] | None = None


async def send_mine_reduce(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineReduce - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineReduceRequest.model_validate(params or {})
    payload = await client._send(
        "MineReduce",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineReduce", payload)


class GraphLearnFitRequest(BaseModel):
    """Request body for GraphLearnFit - contract/schemas/method.request.json#/methods/GraphLearnFit."""

    model_config = ConfigDict(extra="forbid")

    params: Any | None = None
    source: Any
    writeback: bool | None = None


async def send_graph_learn_fit(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GraphLearnFit - graphlearn:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    GraphLearnFitRequest.model_validate(params or {})
    payload = await client._send(
        "GraphLearnFit",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GraphLearnFit", payload)


class GraphLearnPredictRequest(BaseModel):
    """Request body for GraphLearnPredict - contract/schemas/method.request.json#/methods/GraphLearnPredict."""

    model_config = ConfigDict(extra="forbid")

    candidate_pairs: list[list[Any]] | None = None
    model: Any
    source: Any
    top_k: int | None = None
    writeback: bool | None = None


async def send_graph_learn_predict(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GraphLearnPredict - graphlearn:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    GraphLearnPredictRequest.model_validate(params or {})
    payload = await client._send(
        "GraphLearnPredict",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GraphLearnPredict", payload)


class MiningPipelineTrainRequest(BaseModel):
    """Request body for MiningPipelineTrain - contract/schemas/method.request.json#/methods/MiningPipelineTrain."""

    model_config = ConfigDict(extra="forbid")

    name: str
    source: Any | None = None
    spec: Any
    writeback: bool | None = None
    x: list[list[float]] | None = None
    y: list[int] | None = None


async def send_mining_pipeline_train(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MiningPipelineTrain - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MiningPipelineTrainRequest.model_validate(params or {})
    payload = await client._send(
        "MiningPipelineTrain",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MiningPipelineTrain", payload)


class MiningPipelineServeRequest(BaseModel):
    """Request body for MiningPipelineServe - contract/schemas/method.request.json#/methods/MiningPipelineServe."""

    model_config = ConfigDict(extra="forbid")

    name: str
    version: int


async def send_mining_pipeline_serve(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MiningPipelineServe - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MiningPipelineServeRequest.model_validate(params or {})
    payload = await client._send(
        "MiningPipelineServe",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MiningPipelineServe", payload)


class MiningPipelinePredictRequest(BaseModel):
    """Request body for MiningPipelinePredict - contract/schemas/method.request.json#/methods/MiningPipelinePredict."""

    model_config = ConfigDict(extra="forbid")

    name: str
    source: Any | None = None
    version: int | None = None
    writeback: bool | None = None
    x: list[list[float]] | None = None


async def send_mining_pipeline_predict(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MiningPipelinePredict - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MiningPipelinePredictRequest.model_validate(params or {})
    payload = await client._send(
        "MiningPipelinePredict",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MiningPipelinePredict", payload)


class MiningPipelineEvaluateRequest(BaseModel):
    """Request body for MiningPipelineEvaluate - contract/schemas/method.request.json#/methods/MiningPipelineEvaluate."""

    model_config = ConfigDict(extra="forbid")

    name: str
    source: Any | None = None
    version: int | None = None
    x: list[list[float]] | None = None
    y: list[int] | None = None


async def send_mining_pipeline_evaluate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MiningPipelineEvaluate - mining:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    MiningPipelineEvaluateRequest.model_validate(params or {})
    payload = await client._send(
        "MiningPipelineEvaluate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MiningPipelineEvaluate", payload)


class MiningPipelineCompareRequest(BaseModel):
    """Request body for MiningPipelineCompare - contract/schemas/method.request.json#/methods/MiningPipelineCompare."""

    model_config = ConfigDict(extra="forbid")

    name: str
    version_a: int
    version_b: int


async def send_mining_pipeline_compare(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MiningPipelineCompare - mining:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    MiningPipelineCompareRequest.model_validate(params or {})
    payload = await client._send(
        "MiningPipelineCompare",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MiningPipelineCompare", payload)


class MineSequenceRequest(BaseModel):
    """Request body for MineSequence - contract/schemas/method.request.json#/methods/MineSequence."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    as_claim: bool | None = None
    min_support: float | None = None
    sequences: list[list[str]] | None = None
    source: Any | None = None
    writeback: bool | None = None


async def send_mine_sequence(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineSequence - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineSequenceRequest.model_validate(params or {})
    payload = await client._send(
        "MineSequence",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineSequence", payload)


class MineForecastRequest(BaseModel):
    """Request body for MineForecast - contract/schemas/method.request.json#/methods/MineForecast."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    alpha: float | None = None
    as_claim: bool | None = None
    beta: float | None = None
    confidence: float | None = None
    d: int | None = None
    gamma: float | None = None
    horizon: int | None = None
    p: int | None = None
    period: int | None = None
    q: int | None = None
    series_id: str | None = None
    values: list[float] | None = None
    writeback: bool | None = None


async def send_mine_forecast(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineForecast - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineForecastRequest.model_validate(params or {})
    payload = await client._send(
        "MineForecast",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineForecast", payload)


class MineTextRequest(BaseModel):
    """Request body for MineText - contract/schemas/method.request.json#/methods/MineText."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    alpha: float | None = None
    as_claim: bool | None = None
    beta: float | None = None
    docs: list[list[str]] | None = None
    iterations: int | None = None
    k: int | None = None
    seed: int | None = None
    source: Any | None = None
    top_n: int | None = None
    writeback: bool | None = None


async def send_mine_text(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineText - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineTextRequest.model_validate(params or {})
    payload = await client._send(
        "MineText",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineText", payload)


class MineSubgraphRequest(BaseModel):
    """Request body for MineSubgraph - contract/schemas/method.request.json#/methods/MineSubgraph."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    as_claim: bool | None = None
    label: Any | None = None
    max_edges: int | None = None
    min_support: float | None = None
    writeback: bool | None = None


async def send_mine_subgraph(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineSubgraph - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineSubgraphRequest.model_validate(params or {})
    payload = await client._send(
        "MineSubgraph",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineSubgraph", payload)


class MineEntityResolveRequest(BaseModel):
    """Request body for MineEntityResolve - contract/schemas/method.request.json#/methods/MineEntityResolve."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    block_keys: list[str] | None = None
    bucket_precision: int | None = None
    ids: list[str] | None = None
    records: list[list[str]] | None = None
    source: Any | None = None
    threshold: float | None = None
    vectors: list[list[float]] | None = None
    writeback: bool | None = None


async def send_mine_entity_resolve(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineEntityResolve - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineEntityResolveRequest.model_validate(params or {})
    payload = await client._send(
        "MineEntityResolve",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineEntityResolve", payload)


class MineCausalImpactRequest(BaseModel):
    """Request body for MineCausalImpact - contract/schemas/method.request.json#/methods/MineCausalImpact."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    control: list[float] | None = None
    intervention_index: int | None = None
    series: list[float] | None = None
    series_id: str | None = None
    writeback: bool | None = None


async def send_mine_causal_impact(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineCausalImpact - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineCausalImpactRequest.model_validate(params or {})
    payload = await client._send(
        "MineCausalImpact",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineCausalImpact", payload)


class MineProcessRequest(BaseModel):
    """Request body for MineProcess - contract/schemas/method.request.json#/methods/MineProcess."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    process_id: str | None = None
    traces: list[list[str]] | None = None
    writeback: bool | None = None


async def send_mine_process(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineProcess - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineProcessRequest.model_validate(params or {})
    payload = await client._send(
        "MineProcess",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineProcess", payload)


class MineRootCauseRequest(BaseModel):
    """Request body for MineRootCause - contract/schemas/method.request.json#/methods/MineRootCause."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    decay: float | None = None
    edges: list[list[Any]] | None = None
    max_hops: int | None = None
    nodes: list[str] | None = None
    scores: list[float] | None = None
    symptom: str | None = None
    writeback: bool | None = None


async def send_mine_root_cause(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineRootCause - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineRootCauseRequest.model_validate(params or {})
    payload = await client._send(
        "MineRootCause",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineRootCause", payload)


class MineRiskPropagationRequest(BaseModel):
    """Request body for MineRiskPropagation - contract/schemas/method.request.json#/methods/MineRiskPropagation."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    damping: float | None = None
    edges: list[list[Any]] | None = None
    max_iterations: int | None = None
    nodes: list[str] | None = None
    seed: list[float] | None = None
    tolerance: float | None = None
    writeback: bool | None = None


async def send_mine_risk_propagation(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineRiskPropagation - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineRiskPropagationRequest.model_validate(params or {})
    payload = await client._send(
        "MineRiskPropagation",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineRiskPropagation", payload)


class MineOntologyGapRequest(BaseModel):
    """Request body for MineOntologyGap - contract/schemas/method.request.json#/methods/MineOntologyGap."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    label: Any | None = None
    writeback: bool | None = None


async def send_mine_ontology_gap(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineOntologyGap - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineOntologyGapRequest.model_validate(params or {})
    payload = await client._send(
        "MineOntologyGap",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineOntologyGap", payload)


class MineRetrievalQualityRequest(BaseModel):
    """Request body for MineRetrievalQuality - contract/schemas/method.request.json#/methods/MineRetrievalQuality."""

    model_config = ConfigDict(extra="forbid")

    as_claim: bool | None = None
    k: int | None = None
    query_id: str | None = None
    traces: list[Any] | None = None
    writeback: bool | None = None


async def send_mine_retrieval_quality(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineRetrievalQuality - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineRetrievalQualityRequest.model_validate(params or {})
    payload = await client._send(
        "MineRetrievalQuality",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineRetrievalQuality", payload)


class MineCommunityRequest(BaseModel):
    """Request body for MineCommunity - contract/schemas/method.request.json#/methods/MineCommunity."""

    model_config = ConfigDict(extra="forbid")

    algorithm: Any | None = None
    as_claim: bool | None = None
    label: Any | None = None
    max_iterations: int | None = None
    resolution: float | None = None
    seed: int | None = None
    weighted: bool | None = None
    writeback: bool | None = None


async def send_mine_community(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MineCommunity - mining:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MineCommunityRequest.model_validate(params or {})
    payload = await client._send(
        "MineCommunity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MineCommunity", payload)
