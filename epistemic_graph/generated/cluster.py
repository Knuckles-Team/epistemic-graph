# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated cluster engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import (
    OpaqueResult,
    expect_bool,
    expect_string,
)


class CreateGraphRequest(BaseModel):
    """Request body for CreateGraph - contract/schemas/method.request.json#/methods/CreateGraph."""

    model_config = ConfigDict(extra="forbid")

    graph_name: str
    graph_type: Any


async def send_create_graph(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CreateGraph - graph:admin, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CreateGraphRequest.model_validate(params or {})
    payload = await client._send(
        "CreateGraph",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CreateGraph", payload)


class DeleteGraphRequest(BaseModel):
    """Request body for DeleteGraph - contract/schemas/method.request.json#/methods/DeleteGraph."""

    model_config = ConfigDict(extra="forbid")

    graph_name: str


async def send_delete_graph(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DeleteGraph - graph:admin, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DeleteGraphRequest.model_validate(params or {})
    payload = await client._send(
        "DeleteGraph",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DeleteGraph", payload)


class ListGraphsRequest(BaseModel):
    """Request body for ListGraphs - contract/schemas/method.request.json#/methods/ListGraphs."""

    model_config = ConfigDict(extra="forbid")

    # Method::ListGraphs carries no request fields.


async def send_list_graphs(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ListGraphs - graph:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ListGraphsRequest.model_validate(params or {})
    payload = await client._send(
        "ListGraphs",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ListGraphs", payload)


class ReshardRequest(BaseModel):
    """Request body for Reshard - contract/schemas/method.request.json#/methods/Reshard."""

    model_config = ConfigDict(extra="forbid")

    graph: str
    to_shard: int


async def send_reshard(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Reshard - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReshardRequest.model_validate(params or {})
    payload = await client._send(
        "Reshard",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Reshard", payload)


class CatalogAssignRequest(BaseModel):
    """Request body for CatalogAssign - contract/schemas/method.request.json#/methods/CatalogAssign."""

    model_config = ConfigDict(extra="forbid")

    graph: str
    node: Any | None = None
    shard: int


async def send_catalog_assign(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """CatalogAssign - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CatalogAssignRequest.model_validate(params or {})
    payload = await client._send(
        "CatalogAssign",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("CatalogAssign", payload)


class CatalogReassignRequest(BaseModel):
    """Request body for CatalogReassign - contract/schemas/method.request.json#/methods/CatalogReassign."""

    model_config = ConfigDict(extra="forbid")

    graph: str
    shard: int


async def send_catalog_reassign(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """CatalogReassign - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CatalogReassignRequest.model_validate(params or {})
    payload = await client._send(
        "CatalogReassign",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("CatalogReassign", payload)


class CatalogRemoveRequest(BaseModel):
    """Request body for CatalogRemove - contract/schemas/method.request.json#/methods/CatalogRemove."""

    model_config = ConfigDict(extra="forbid")

    graph: str


async def send_catalog_remove(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """CatalogRemove - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CatalogRemoveRequest.model_validate(params or {})
    payload = await client._send(
        "CatalogRemove",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("CatalogRemove", payload)


class CatalogListRequest(BaseModel):
    """Request body for CatalogList - contract/schemas/method.request.json#/methods/CatalogList."""

    model_config = ConfigDict(extra="forbid")

    # Method::CatalogList carries no request fields.


async def send_catalog_list(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CatalogList - admin:cluster-read, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CatalogListRequest.model_validate(params or {})
    payload = await client._send(
        "CatalogList",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CatalogList", payload)


class RebalancePlanRequest(BaseModel):
    """Request body for RebalancePlan - contract/schemas/method.request.json#/methods/RebalancePlan."""

    model_config = ConfigDict(extra="forbid")

    max_moves: Any | None = None
    tolerance: Any | None = None


async def send_rebalance_plan(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RebalancePlan - admin:cluster-read, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    RebalancePlanRequest.model_validate(params or {})
    payload = await client._send(
        "RebalancePlan",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RebalancePlan", payload)


class RebalanceExecuteRequest(BaseModel):
    """Request body for RebalanceExecute - contract/schemas/method.request.json#/methods/RebalanceExecute."""

    model_config = ConfigDict(extra="forbid")

    max_moves: Any | None = None
    tolerance: Any | None = None


async def send_rebalance_execute(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RebalanceExecute - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RebalanceExecuteRequest.model_validate(params or {})
    payload = await client._send(
        "RebalanceExecute",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RebalanceExecute", payload)


class PlacementRouteRequest(BaseModel):
    """Request body for PlacementRoute - contract/schemas/method.request.json#/methods/PlacementRoute."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_placement_route(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PlacementRoute - cluster:placement-read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    PlacementRouteRequest.model_validate(params or {})
    payload = await client._send(
        "PlacementRoute",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PlacementRoute", payload)


class RaftAddLearnerRequest(BaseModel):
    """Request body for RaftAddLearner - contract/schemas/method.request.json#/methods/RaftAddLearner."""

    model_config = ConfigDict(extra="forbid")

    addr: str
    group: Any | None = None
    node_id: int


async def send_raft_add_learner(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """RaftAddLearner - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RaftAddLearnerRequest.model_validate(params or {})
    payload = await client._send(
        "RaftAddLearner",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("RaftAddLearner", payload)


class RaftChangeMembershipRequest(BaseModel):
    """Request body for RaftChangeMembership - contract/schemas/method.request.json#/methods/RaftChangeMembership."""

    model_config = ConfigDict(extra="forbid")

    group: Any | None = None
    voters: list[int]


async def send_raft_change_membership(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """RaftChangeMembership - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RaftChangeMembershipRequest.model_validate(params or {})
    payload = await client._send(
        "RaftChangeMembership",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("RaftChangeMembership", payload)


class ClusterMembersRequest(BaseModel):
    """Request body for ClusterMembers - contract/schemas/method.request.json#/methods/ClusterMembers."""

    model_config = ConfigDict(extra="forbid")

    # Method::ClusterMembers carries no request fields.


async def send_cluster_members(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ClusterMembers - cluster:topology-read, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ClusterMembersRequest.model_validate(params or {})
    payload = await client._send(
        "ClusterMembers",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ClusterMembers", payload)


class RegisterServerRequest(BaseModel):
    """Request body for RegisterServer - contract/schemas/method.request.json#/methods/RegisterServer."""

    model_config = ConfigDict(extra="forbid")

    name: str
    resources_json: str | None = None
    ttl_secs: int
    url: str


async def send_register_server(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RegisterServer - registry:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RegisterServerRequest.model_validate(params or {})
    payload = await client._send(
        "RegisterServer",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RegisterServer", payload)


class PlacementAdminRequest(BaseModel):
    """Request body for PlacementAdmin - contract/schemas/method.request.json#/methods/PlacementAdmin."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_placement_admin(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PlacementAdmin - admin:cluster, ControlRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PlacementAdminRequest.model_validate(params or {})
    payload = await client._send(
        "PlacementAdmin",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PlacementAdmin", payload)


class PingRequest(BaseModel):
    """Request body for Ping - contract/schemas/method.request.json#/methods/Ping."""

    model_config = ConfigDict(extra="forbid")

    # Method::Ping carries no request fields.


async def send_ping(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """Ping - service:control, None, replay NotReplayable.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    PingRequest.model_validate(params or {})
    payload = await client._send(
        "Ping",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("Ping", payload)


class HealthRequest(BaseModel):
    """Request body for Health - contract/schemas/method.request.json#/methods/Health."""

    model_config = ConfigDict(extra="forbid")

    # Method::Health carries no request fields.


async def send_health(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Health - service:control, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    HealthRequest.model_validate(params or {})
    payload = await client._send(
        "Health",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Health", payload)


class ShutdownRequest(BaseModel):
    """Request body for Shutdown - contract/schemas/method.request.json#/methods/Shutdown."""

    model_config = ConfigDict(extra="forbid")

    # Method::Shutdown carries no request fields.


async def send_shutdown(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """Shutdown - service:admin, VolatileControl, replay NonceOnly.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT.
    """
    ShutdownRequest.model_validate(params or {})
    payload = await client._send(
        "Shutdown",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("Shutdown", payload)


class CancelRequestRequest(BaseModel):
    """Request body for CancelRequest - contract/schemas/method.request.json#/methods/CancelRequest."""

    model_config = ConfigDict(extra="forbid")

    target_req_id: int


async def send_cancel_request(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """CancelRequest - service:control, None, replay NotReplayable.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CancelRequestRequest.model_validate(params or {})
    payload = await client._send(
        "CancelRequest",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("CancelRequest", payload)


class RegisterForeignSourceRequest(BaseModel):
    """Request body for RegisterForeignSource - contract/schemas/method.request.json#/methods/RegisterForeignSource."""

    model_config = ConfigDict(extra="forbid")

    name: str
    source: Any


async def send_register_foreign_source(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """RegisterForeignSource - federation:admin, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RegisterForeignSourceRequest.model_validate(params or {})
    payload = await client._send(
        "RegisterForeignSource",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("RegisterForeignSource", payload)


class PlanMatViewDefineRequest(BaseModel):
    """Request body for PlanMatViewDefine - contract/schemas/method.request.json#/methods/PlanMatViewDefine."""

    model_config = ConfigDict(extra="forbid")

    graph: str
    name: str
    plan: Any


async def send_plan_mat_view_define(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PlanMatViewDefine - matview:admin, ControlRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PlanMatViewDefineRequest.model_validate(params or {})
    payload = await client._send(
        "PlanMatViewDefine",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PlanMatViewDefine", payload)


class PlanMatViewGetRequest(BaseModel):
    """Request body for PlanMatViewGet - contract/schemas/method.request.json#/methods/PlanMatViewGet."""

    model_config = ConfigDict(extra="forbid")

    name: str


async def send_plan_mat_view_get(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PlanMatViewGet - matview:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    PlanMatViewGetRequest.model_validate(params or {})
    payload = await client._send(
        "PlanMatViewGet",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PlanMatViewGet", payload)


class PlanMatViewRefreshRequest(BaseModel):
    """Request body for PlanMatViewRefresh - contract/schemas/method.request.json#/methods/PlanMatViewRefresh."""

    model_config = ConfigDict(extra="forbid")

    name: str


async def send_plan_mat_view_refresh(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PlanMatViewRefresh - matview:admin, ControlRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PlanMatViewRefreshRequest.model_validate(params or {})
    payload = await client._send(
        "PlanMatViewRefresh",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PlanMatViewRefresh", payload)


class PlanMatViewDropRequest(BaseModel):
    """Request body for PlanMatViewDrop - contract/schemas/method.request.json#/methods/PlanMatViewDrop."""

    model_config = ConfigDict(extra="forbid")

    name: str


async def send_plan_mat_view_drop(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PlanMatViewDrop - matview:admin, ControlRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PlanMatViewDropRequest.model_validate(params or {})
    payload = await client._send(
        "PlanMatViewDrop",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PlanMatViewDrop", payload)
