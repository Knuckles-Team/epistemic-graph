# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated query engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import OpaqueResult


class GetContextViewRequest(BaseModel):
    """Request body for GetContextView - contract/schemas/method.request.json#/methods/GetContextView."""

    model_config = ConfigDict(extra="forbid")

    agent_id: str
    max_tokens: int


async def send_get_context_view(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetContextView - node:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetContextViewRequest.model_validate(params or {})
    payload = await client._send(
        "GetContextView",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetContextView", payload)


class GetChangeEnvelopeRequest(BaseModel):
    """Request body for GetChangeEnvelope - contract/schemas/method.request.json#/methods/GetChangeEnvelope."""

    model_config = ConfigDict(extra="forbid")

    envelope_id: str
    tenant: str


async def send_get_change_envelope(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetChangeEnvelope - ingest:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetChangeEnvelopeRequest.model_validate(params or {})
    payload = await client._send(
        "GetChangeEnvelope",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetChangeEnvelope", payload)


class GetContentVersionRequest(BaseModel):
    """Request body for GetContentVersion - contract/schemas/method.request.json#/methods/GetContentVersion."""

    model_config = ConfigDict(extra="forbid")

    object_id: str
    tenant: str


async def send_get_content_version(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetContentVersion - ingest:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetContentVersionRequest.model_validate(params or {})
    payload = await client._send(
        "GetContentVersion",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetContentVersion", payload)


class GetChangeCursorRequest(BaseModel):
    """Request body for GetChangeCursor - contract/schemas/method.request.json#/methods/GetChangeCursor."""

    model_config = ConfigDict(extra="forbid")

    partition: str | None = None
    source: str
    tenant: str


async def send_get_change_cursor(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetChangeCursor - ingest:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetChangeCursorRequest.model_validate(params or {})
    payload = await client._send(
        "GetChangeCursor",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetChangeCursor", payload)


class SqlRequest(BaseModel):
    """Request body for Sql - contract/schemas/method.request.json#/methods/Sql."""

    model_config = ConfigDict(extra="forbid")

    params_msgpack: bytes | None = None
    query: str


async def send_sql(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Sql - query:sql, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    SqlRequest.model_validate(params or {})
    payload = await client._send(
        "Sql",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Sql", payload)


class CypherQueryRequest(BaseModel):
    """Request body for CypherQuery - contract/schemas/method.request.json#/methods/CypherQuery."""

    model_config = ConfigDict(extra="forbid")

    mode: Any
    query: str


async def send_cypher_query(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CypherQuery - query:cypher, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CypherQueryRequest.model_validate(params or {})
    payload = await client._send(
        "CypherQuery",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CypherQuery", payload)


class GraphQlRequest(BaseModel):
    """Request body for GraphQl - contract/schemas/method.request.json#/methods/GraphQl."""

    model_config = ConfigDict(extra="forbid")

    query: str
    variables: Any | None = None


async def send_graph_ql(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GraphQl - query:graphql, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    GraphQlRequest.model_validate(params or {})
    payload = await client._send(
        "GraphQl",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GraphQl", payload)


class KnowledgeStreamRequest(BaseModel):
    """Request body for KnowledgeStream - contract/schemas/method.request.json#/methods/KnowledgeStream."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_knowledge_stream(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """KnowledgeStream - query:stream, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    KnowledgeStreamRequest.model_validate(params or {})
    payload = await client._send(
        "KnowledgeStream",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("KnowledgeStream", payload)


class UnifiedQueryRequest(BaseModel):
    """Request body for UnifiedQuery - contract/schemas/method.request.json#/methods/UnifiedQuery."""

    model_config = ConfigDict(extra="forbid")

    plan: Any


async def send_unified_query(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """UnifiedQuery - query:unified, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    UnifiedQueryRequest.model_validate(params or {})
    payload = await client._send(
        "UnifiedQuery",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("UnifiedQuery", payload)


class UnifiedQueryTextRequest(BaseModel):
    """Request body for UnifiedQueryText - contract/schemas/method.request.json#/methods/UnifiedQueryText."""

    model_config = ConfigDict(extra="forbid")

    text: str


async def send_unified_query_text(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """UnifiedQueryText - query:unified, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    UnifiedQueryTextRequest.model_validate(params or {})
    payload = await client._send(
        "UnifiedQueryText",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("UnifiedQueryText", payload)


class ExplainPlanRequest(BaseModel):
    """Request body for ExplainPlan - contract/schemas/method.request.json#/methods/ExplainPlan."""

    model_config = ConfigDict(extra="forbid")

    plan: Any


async def send_explain_plan(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ExplainPlan - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ExplainPlanRequest.model_validate(params or {})
    payload = await client._send(
        "ExplainPlan",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ExplainPlan", payload)


class ExplainProvenanceRequest(BaseModel):
    """Request body for ExplainProvenance - contract/schemas/method.request.json#/methods/ExplainProvenance."""

    model_config = ConfigDict(extra="forbid")

    plan: Any


async def send_explain_provenance(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ExplainProvenance - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ExplainProvenanceRequest.model_validate(params or {})
    payload = await client._send(
        "ExplainProvenance",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ExplainProvenance", payload)


class ExplainProvenanceByIdsRequest(BaseModel):
    """Request body for ExplainProvenanceByIds - contract/schemas/method.request.json#/methods/ExplainProvenanceByIds."""

    model_config = ConfigDict(extra="forbid")

    ids: list[str]


async def send_explain_provenance_by_ids(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ExplainProvenanceByIds - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ExplainProvenanceByIdsRequest.model_validate(params or {})
    payload = await client._send(
        "ExplainProvenanceByIds",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ExplainProvenanceByIds", payload)


class ExplainPolicyRequest(BaseModel):
    """Request body for ExplainPolicy - contract/schemas/method.request.json#/methods/ExplainPolicy."""

    model_config = ConfigDict(extra="forbid")

    plan: Any


async def send_explain_policy(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ExplainPolicy - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ExplainPolicyRequest.model_validate(params or {})
    payload = await client._send(
        "ExplainPolicy",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ExplainPolicy", payload)


class ExplainBeliefRequest(BaseModel):
    """Request body for ExplainBelief - contract/schemas/method.request.json#/methods/ExplainBelief."""

    model_config = ConfigDict(extra="forbid")

    disclosure_level: Any | None | None = None
    node_id: str


async def send_explain_belief(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ExplainBelief - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ExplainBeliefRequest.model_validate(params or {})
    payload = await client._send(
        "ExplainBelief",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ExplainBelief", payload)


class EpistemicStatusRequest(BaseModel):
    """Request body for EpistemicStatus - contract/schemas/method.request.json#/methods/EpistemicStatus."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_epistemic_status(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """EpistemicStatus - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    EpistemicStatusRequest.model_validate(params or {})
    payload = await client._send(
        "EpistemicStatus",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("EpistemicStatus", payload)


class WhatChangedRequest(BaseModel):
    """Request body for WhatChanged - contract/schemas/method.request.json#/methods/WhatChanged."""

    model_config = ConfigDict(extra="forbid")

    tx_from: int
    tx_to: int


async def send_what_changed(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """WhatChanged - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    WhatChangedRequest.model_validate(params or {})
    payload = await client._send(
        "WhatChanged",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("WhatChanged", payload)


class RecomputeMaterializationRequest(BaseModel):
    """Request body for RecomputeMaterialization - contract/schemas/method.request.json#/methods/RecomputeMaterialization."""

    model_config = ConfigDict(extra="forbid")

    derived_id: str
    expected_source_graph_version: int


async def send_recompute_materialization(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RecomputeMaterialization - reasoning:write, ReasoningProjection, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RecomputeMaterializationRequest.model_validate(params or {})
    payload = await client._send(
        "RecomputeMaterialization",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RecomputeMaterialization", payload)


class MaterializationStatusRequest(BaseModel):
    """Request body for MaterializationStatus - contract/schemas/method.request.json#/methods/MaterializationStatus."""

    model_config = ConfigDict(extra="forbid")

    id: str


async def send_materialization_status(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MaterializationStatus - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    MaterializationStatusRequest.model_validate(params or {})
    payload = await client._send(
        "MaterializationStatus",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MaterializationStatus", payload)


class StaleMaterializationsRequest(BaseModel):
    """Request body for StaleMaterializations - contract/schemas/method.request.json#/methods/StaleMaterializations."""

    model_config = ConfigDict(extra="forbid")

    # Method::StaleMaterializations carries no request fields.


async def send_stale_materializations(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """StaleMaterializations - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    StaleMaterializationsRequest.model_validate(params or {})
    payload = await client._send(
        "StaleMaterializations",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("StaleMaterializations", payload)


class ResolveConflictRequest(BaseModel):
    """Request body for ResolveConflict - contract/schemas/method.request.json#/methods/ResolveConflict."""

    model_config = ConfigDict(extra="forbid")

    node_ids: list[str]
    semantics: str | None = None


async def send_resolve_conflict(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ResolveConflict - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ResolveConflictRequest.model_validate(params or {})
    payload = await client._send(
        "ResolveConflict",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ResolveConflict", payload)


class ExplainEvidenceRequest(BaseModel):
    """Request body for ExplainEvidence - contract/schemas/method.request.json#/methods/ExplainEvidence."""

    model_config = ConfigDict(extra="forbid")

    node_id: str


async def send_explain_evidence(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ExplainEvidence - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ExplainEvidenceRequest.model_validate(params or {})
    payload = await client._send(
        "ExplainEvidence",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ExplainEvidence", payload)


class CausalEstimateRequest(BaseModel):
    """Request body for CausalEstimate - contract/schemas/method.request.json#/methods/CausalEstimate."""

    model_config = ConfigDict(extra="forbid")

    do_values: dict[str, float]
    mode: Any
    variables: list[Any]


async def send_causal_estimate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CausalEstimate - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CausalEstimateRequest.model_validate(params or {})
    payload = await client._send(
        "CausalEstimate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CausalEstimate", payload)


class CausalCounterfactualRequest(BaseModel):
    """Request body for CausalCounterfactual - contract/schemas/method.request.json#/methods/CausalCounterfactual."""

    model_config = ConfigDict(extra="forbid")

    actual: dict[str, float]
    do_values: dict[str, float]
    variables: list[Any]


async def send_causal_counterfactual(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CausalCounterfactual - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CausalCounterfactualRequest.model_validate(params or {})
    payload = await client._send(
        "CausalCounterfactual",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CausalCounterfactual", payload)


class RankByProvenanceRequest(BaseModel):
    """Request body for RankByProvenance - contract/schemas/method.request.json#/methods/RankByProvenance."""

    model_config = ConfigDict(extra="forbid")

    candidates: list[Any]
    weights: Any | None = None


async def send_rank_by_provenance(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RankByProvenance - explain:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    RankByProvenanceRequest.model_validate(params or {})
    payload = await client._send(
        "RankByProvenance",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RankByProvenance", payload)


class NlQueryRequest(BaseModel):
    """Request body for NlQuery - contract/schemas/method.request.json#/methods/NlQuery."""

    model_config = ConfigDict(extra="forbid")

    graph: str | None = None
    text: str


async def send_nl_query(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """NlQuery - query:nl, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    NlQueryRequest.model_validate(params or {})
    payload = await client._send(
        "NlQuery",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("NlQuery", payload)


class TxnUnifiedQueryRequest(BaseModel):
    """Request body for TxnUnifiedQuery - contract/schemas/method.request.json#/methods/TxnUnifiedQuery."""

    model_config = ConfigDict(extra="forbid")

    plan: Any
    txn_id: str


async def send_txn_unified_query(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """TxnUnifiedQuery - txn:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TxnUnifiedQueryRequest.model_validate(params or {})
    payload = await client._send(
        "TxnUnifiedQuery",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("TxnUnifiedQuery", payload)


class TxnUnifiedQueryTextRequest(BaseModel):
    """Request body for TxnUnifiedQueryText - contract/schemas/method.request.json#/methods/TxnUnifiedQueryText."""

    model_config = ConfigDict(extra="forbid")

    text: str
    txn_id: str


async def send_txn_unified_query_text(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """TxnUnifiedQueryText - txn:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TxnUnifiedQueryTextRequest.model_validate(params or {})
    payload = await client._send(
        "TxnUnifiedQueryText",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("TxnUnifiedQueryText", payload)
