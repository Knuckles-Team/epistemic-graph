# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated messaging engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import (
    OpaqueResult,
    expect_bool,
    expect_count,
    expect_ids,
    expect_string,
)


class DeclareExchangeRequest(BaseModel):
    """Request body for DeclareExchange - contract/schemas/method.request.json#/methods/DeclareExchange."""

    model_config = ConfigDict(extra="forbid")

    exchange: str
    kind: str


async def send_declare_exchange(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """DeclareExchange - broker:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/DeclareExchange).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DeclareExchangeRequest.model_validate(params or {})
    payload = await client._send(
        "DeclareExchange",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("DeclareExchange", payload)


class DeleteExchangeRequest(BaseModel):
    """Request body for DeleteExchange - contract/schemas/method.request.json#/methods/DeleteExchange."""

    model_config = ConfigDict(extra="forbid")

    exchange: str


async def send_delete_exchange(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """DeleteExchange - broker:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/DeleteExchange).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DeleteExchangeRequest.model_validate(params or {})
    payload = await client._send(
        "DeleteExchange",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("DeleteExchange", payload)


class BindQueueRequest(BaseModel):
    """Request body for BindQueue - contract/schemas/method.request.json#/methods/BindQueue."""

    model_config = ConfigDict(extra="forbid")

    exchange: str
    queue: str
    routing_key: str


async def send_bind_queue(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """BindQueue - broker:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/BindQueue).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BindQueueRequest.model_validate(params or {})
    payload = await client._send(
        "BindQueue",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("BindQueue", payload)


class UnbindQueueRequest(BaseModel):
    """Request body for UnbindQueue - contract/schemas/method.request.json#/methods/UnbindQueue."""

    model_config = ConfigDict(extra="forbid")

    exchange: str
    queue: str
    routing_key: str


async def send_unbind_queue(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """UnbindQueue - broker:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/UnbindQueue).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    UnbindQueueRequest.model_validate(params or {})
    payload = await client._send(
        "UnbindQueue",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("UnbindQueue", payload)


class PublishRequest(BaseModel):
    """Request body for Publish - contract/schemas/method.request.json#/methods/Publish."""

    model_config = ConfigDict(extra="forbid")

    exchange: str
    payload: bytes
    routing_key: str


async def send_publish(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """Publish - broker:publish, Outbox, replay OperationIdentity.

    Result: ResultPayload::Count (contract/schemas/result.messaging.json#/methods/Publish).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PublishRequest.model_validate(params or {})
    payload = await client._send(
        "Publish",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_count("Publish", payload)


class DeclareQueueRequest(BaseModel):
    """Request body for DeclareQueue - contract/schemas/method.request.json#/methods/DeclareQueue."""

    model_config = ConfigDict(extra="forbid")

    dl_exchange: Any | None = None
    dl_routing_key: Any | None = None
    max_delivery_count: Any | None = None
    max_priority: Any | None = None
    message_ttl_ms: Any | None = None
    queue: str
    queue_expiry_ms: Any | None = None


async def send_declare_queue(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """DeclareQueue - broker:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/DeclareQueue).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DeclareQueueRequest.model_validate(params or {})
    payload = await client._send(
        "DeclareQueue",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("DeclareQueue", payload)


class PublishExRequest(BaseModel):
    """Request body for PublishEx - contract/schemas/method.request.json#/methods/PublishEx."""

    model_config = ConfigDict(extra="forbid")

    delay_ms: Any | None = None
    exchange: str
    now_ms: Any | None = None
    payload: bytes
    priority: int | None = None
    routing_key: str
    ttl_ms: Any | None = None


async def send_publish_ex(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """PublishEx - broker:publish, Outbox, replay OperationIdentity.

    Result: ResultPayload::Count (contract/schemas/result.messaging.json#/methods/PublishEx).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PublishExRequest.model_validate(params or {})
    payload = await client._send(
        "PublishEx",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_count("PublishEx", payload)


class BrokerConsumeRequest(BaseModel):
    """Request body for BrokerConsume - contract/schemas/method.request.json#/methods/BrokerConsume."""

    model_config = ConfigDict(extra="forbid")

    consumer: str
    group: str
    lease_ms: int
    now_ms: int
    prefetch: int
    queue: str


async def send_broker_consume(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BrokerConsume - broker:consume, Outbox, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BrokerConsumeRequest.model_validate(params or {})
    payload = await client._send(
        "BrokerConsume",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BrokerConsume", payload)


class BrokerAckRequest(BaseModel):
    """Request body for BrokerAck - contract/schemas/method.request.json#/methods/BrokerAck."""

    model_config = ConfigDict(extra="forbid")

    node_id: str
    queue: str


async def send_broker_ack(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """BrokerAck - broker:ack, Outbox, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/BrokerAck).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BrokerAckRequest.model_validate(params or {})
    payload = await client._send(
        "BrokerAck",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("BrokerAck", payload)


class BrokerRejectRequest(BaseModel):
    """Request body for BrokerReject - contract/schemas/method.request.json#/methods/BrokerReject."""

    model_config = ConfigDict(extra="forbid")

    node_id: str
    now_ms: int
    queue: str
    requeue: bool


async def send_broker_reject(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """BrokerReject - broker:ack, Outbox, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/BrokerReject).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BrokerRejectRequest.model_validate(params or {})
    payload = await client._send(
        "BrokerReject",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("BrokerReject", payload)


class SweepExpiredRequest(BaseModel):
    """Request body for SweepExpired - contract/schemas/method.request.json#/methods/SweepExpired."""

    model_config = ConfigDict(extra="forbid")

    now_ms: int


async def send_sweep_expired(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """SweepExpired - broker:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::Count (contract/schemas/result.messaging.json#/methods/SweepExpired).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    SweepExpiredRequest.model_validate(params or {})
    payload = await client._send(
        "SweepExpired",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_count("SweepExpired", payload)


class StreamDeclareRequest(BaseModel):
    """Request body for StreamDeclare - contract/schemas/method.request.json#/methods/StreamDeclare."""

    model_config = ConfigDict(extra="forbid")

    max_age_ms: Any | None = None
    max_messages: Any | None = None
    stream: str


async def send_stream_declare(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """StreamDeclare - stream:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/StreamDeclare).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    StreamDeclareRequest.model_validate(params or {})
    payload = await client._send(
        "StreamDeclare",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("StreamDeclare", payload)


class StreamPublishRequest(BaseModel):
    """Request body for StreamPublish - contract/schemas/method.request.json#/methods/StreamPublish."""

    model_config = ConfigDict(extra="forbid")

    now_ms: int
    payload: bytes
    stream: str


async def send_stream_publish(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """StreamPublish - stream:write, Outbox, replay OperationIdentity.

    Result: ResultPayload::Count (contract/schemas/result.messaging.json#/methods/StreamPublish).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    StreamPublishRequest.model_validate(params or {})
    payload = await client._send(
        "StreamPublish",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_count("StreamPublish", payload)


class StreamReadRequest(BaseModel):
    """Request body for StreamRead - contract/schemas/method.request.json#/methods/StreamRead."""

    model_config = ConfigDict(extra="forbid")

    from_offset: int
    max: int
    stream: str


async def send_stream_read(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """StreamRead - stream:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    StreamReadRequest.model_validate(params or {})
    payload = await client._send(
        "StreamRead",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("StreamRead", payload)


class StreamTrimRequest(BaseModel):
    """Request body for StreamTrim - contract/schemas/method.request.json#/methods/StreamTrim."""

    model_config = ConfigDict(extra="forbid")

    now_ms: int
    stream: str


async def send_stream_trim(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """StreamTrim - stream:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::Count (contract/schemas/result.messaging.json#/methods/StreamTrim).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    StreamTrimRequest.model_validate(params or {})
    payload = await client._send(
        "StreamTrim",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_count("StreamTrim", payload)


class StreamCommitOffsetRequest(BaseModel):
    """Request body for StreamCommitOffset - contract/schemas/method.request.json#/methods/StreamCommitOffset."""

    model_config = ConfigDict(extra="forbid")

    group: str
    offset: int
    stream: str


async def send_stream_commit_offset(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """StreamCommitOffset - stream:admin, Outbox, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/StreamCommitOffset).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    StreamCommitOffsetRequest.model_validate(params or {})
    payload = await client._send(
        "StreamCommitOffset",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("StreamCommitOffset", payload)


class StreamCommittedOffsetRequest(BaseModel):
    """Request body for StreamCommittedOffset - contract/schemas/method.request.json#/methods/StreamCommittedOffset."""

    model_config = ConfigDict(extra="forbid")

    group: str
    stream: str


async def send_stream_committed_offset(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """StreamCommittedOffset - stream:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    StreamCommittedOffsetRequest.model_validate(params or {})
    payload = await client._send(
        "StreamCommittedOffset",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("StreamCommittedOffset", payload)


class PublishConfirmedRequest(BaseModel):
    """Request body for PublishConfirmed - contract/schemas/method.request.json#/methods/PublishConfirmed."""

    model_config = ConfigDict(extra="forbid")

    delay_ms: Any | None = None
    exchange: str
    now_ms: Any | None = None
    payload: bytes
    priority: int | None = None
    routing_key: str
    ttl_ms: Any | None = None


async def send_publish_confirmed(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PublishConfirmed - broker:publish, Outbox, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PublishConfirmedRequest.model_validate(params or {})
    payload = await client._send(
        "PublishConfirmed",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PublishConfirmed", payload)


class PublishIdempotentRequest(BaseModel):
    """Request body for PublishIdempotent - contract/schemas/method.request.json#/methods/PublishIdempotent."""

    model_config = ConfigDict(extra="forbid")

    delay_ms: Any | None = None
    exchange: str
    now_ms: Any | None = None
    payload: bytes
    priority: int | None = None
    producer_id: Any | None = None
    routing_key: str
    seq: int | None = None
    ttl_ms: Any | None = None


async def send_publish_idempotent(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """PublishIdempotent - broker:publish, Outbox, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    PublishIdempotentRequest.model_validate(params or {})
    payload = await client._send(
        "PublishIdempotent",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("PublishIdempotent", payload)


class BrokerAckTagRequest(BaseModel):
    """Request body for BrokerAckTag - contract/schemas/method.request.json#/methods/BrokerAckTag."""

    model_config = ConfigDict(extra="forbid")

    consumer: str
    delivery_tag: int


async def send_broker_ack_tag(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """BrokerAckTag - broker:ack, Outbox, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/BrokerAckTag).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BrokerAckTagRequest.model_validate(params or {})
    payload = await client._send(
        "BrokerAckTag",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("BrokerAckTag", payload)


class BrokerNackTagRequest(BaseModel):
    """Request body for BrokerNackTag - contract/schemas/method.request.json#/methods/BrokerNackTag."""

    model_config = ConfigDict(extra="forbid")

    consumer: str
    delivery_tag: int
    now_ms: int
    requeue: bool


async def send_broker_nack_tag(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """BrokerNackTag - broker:ack, Outbox, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/BrokerNackTag).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BrokerNackTagRequest.model_validate(params or {})
    payload = await client._send(
        "BrokerNackTag",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("BrokerNackTag", payload)


class BrokerRenewTagRequest(BaseModel):
    """Request body for BrokerRenewTag - contract/schemas/method.request.json#/methods/BrokerRenewTag."""

    model_config = ConfigDict(extra="forbid")

    consumer: str
    delivery_tag: int
    lease_ms: int
    now_ms: int


async def send_broker_renew_tag(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """BrokerRenewTag - broker:ack, Outbox, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/BrokerRenewTag).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BrokerRenewTagRequest.model_validate(params or {})
    payload = await client._send(
        "BrokerRenewTag",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("BrokerRenewTag", payload)


class CreateChannelRequest(BaseModel):
    """Request body for CreateChannel - contract/schemas/method.request.json#/methods/CreateChannel."""

    model_config = ConfigDict(extra="forbid")

    channel_id: str
    channel_type: Any
    creator: str
    initial_members: list[str]


async def send_create_channel(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CreateChannel - channel:admin, ControlRedb, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CreateChannelRequest.model_validate(params or {})
    payload = await client._send(
        "CreateChannel",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CreateChannel", payload)


class JoinChannelRequest(BaseModel):
    """Request body for JoinChannel - contract/schemas/method.request.json#/methods/JoinChannel."""

    model_config = ConfigDict(extra="forbid")

    agent_id: str
    channel_id: str


async def send_join_channel(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """JoinChannel - channel:admin, ControlRedb, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    JoinChannelRequest.model_validate(params or {})
    payload = await client._send(
        "JoinChannel",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("JoinChannel", payload)


class LeaveChannelRequest(BaseModel):
    """Request body for LeaveChannel - contract/schemas/method.request.json#/methods/LeaveChannel."""

    model_config = ConfigDict(extra="forbid")

    agent_id: str
    channel_id: str


async def send_leave_channel(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """LeaveChannel - channel:admin, ControlRedb, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    LeaveChannelRequest.model_validate(params or {})
    payload = await client._send(
        "LeaveChannel",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("LeaveChannel", payload)


class CloseChannelRequest(BaseModel):
    """Request body for CloseChannel - contract/schemas/method.request.json#/methods/CloseChannel."""

    model_config = ConfigDict(extra="forbid")

    channel_id: str
    summary_embedding: Any | None = None
    topic_metadata: Any | None = None


async def send_close_channel(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CloseChannel - channel:admin, ControlRedb, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CloseChannelRequest.model_validate(params or {})
    payload = await client._send(
        "CloseChannel",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CloseChannel", payload)


class SendMessageRequest(BaseModel):
    """Request body for SendMessage - contract/schemas/method.request.json#/methods/SendMessage."""

    model_config = ConfigDict(extra="forbid")

    channel_id: str
    payload: str
    sender: str


async def send_send_message(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """SendMessage - channel:write, ControlRedb, replay OperationIdentity.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    SendMessageRequest.model_validate(params or {})
    payload = await client._send(
        "SendMessage",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("SendMessage", payload)


class GetChannelMessagesRequest(BaseModel):
    """Request body for GetChannelMessages - contract/schemas/method.request.json#/methods/GetChannelMessages."""

    model_config = ConfigDict(extra="forbid")

    channel_id: str
    limit: Any | None = None


async def send_get_channel_messages(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetChannelMessages - channel:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetChannelMessagesRequest.model_validate(params or {})
    payload = await client._send(
        "GetChannelMessages",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetChannelMessages", payload)


class ListChannelsRequest(BaseModel):
    """Request body for ListChannels - contract/schemas/method.request.json#/methods/ListChannels."""

    model_config = ConfigDict(extra="forbid")

    # Method::ListChannels carries no request fields.


async def send_list_channels(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ListChannels - channel:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ListChannelsRequest.model_validate(params or {})
    payload = await client._send(
        "ListChannels",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ListChannels", payload)


class GetChannelMembersRequest(BaseModel):
    """Request body for GetChannelMembers - contract/schemas/method.request.json#/methods/GetChannelMembers."""

    model_config = ConfigDict(extra="forbid")

    channel_id: str


async def send_get_channel_members(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> list[str]:
    """GetChannelMembers - channel:read, None, replay NotReplayable.

    Result: ResultPayload::Ids (contract/schemas/result.messaging.json#/methods/GetChannelMembers).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetChannelMembersRequest.model_validate(params or {})
    payload = await client._send(
        "GetChannelMembers",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_ids("GetChannelMembers", payload)


class CdcReadRequest(BaseModel):
    """Request body for CdcRead - contract/schemas/method.request.json#/methods/CdcRead."""

    model_config = ConfigDict(extra="forbid")

    from_seq: int
    graph: str
    limit: int | None = None


async def send_cdc_read(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CdcRead - cdc:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CdcReadRequest.model_validate(params or {})
    payload = await client._send(
        "CdcRead",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CdcRead", payload)


class RegisterContinuousQueryRequest(BaseModel):
    """Request body for RegisterContinuousQuery - contract/schemas/method.request.json#/methods/RegisterContinuousQuery."""

    model_config = ConfigDict(extra="forbid")

    name: str
    spec_msgpack: bytes


async def send_register_continuous_query(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """RegisterContinuousQuery - cdc:admin, ControlRedb, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/RegisterContinuousQuery).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RegisterContinuousQueryRequest.model_validate(params or {})
    payload = await client._send(
        "RegisterContinuousQuery",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("RegisterContinuousQuery", payload)


class ReadContinuousQueryRequest(BaseModel):
    """Request body for ReadContinuousQuery - contract/schemas/method.request.json#/methods/ReadContinuousQuery."""

    model_config = ConfigDict(extra="forbid")

    name: str


async def send_read_continuous_query(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReadContinuousQuery - cdc:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ReadContinuousQueryRequest.model_validate(params or {})
    payload = await client._send(
        "ReadContinuousQuery",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReadContinuousQuery", payload)


class DropContinuousQueryRequest(BaseModel):
    """Request body for DropContinuousQuery - contract/schemas/method.request.json#/methods/DropContinuousQuery."""

    model_config = ConfigDict(extra="forbid")

    name: str


async def send_drop_continuous_query(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """DropContinuousQuery - cdc:admin, ControlRedb, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/DropContinuousQuery).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DropContinuousQueryRequest.model_validate(params or {})
    payload = await client._send(
        "DropContinuousQuery",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("DropContinuousQuery", payload)


class WatchRequest(BaseModel):
    """Request body for Watch - contract/schemas/method.request.json#/methods/Watch."""

    model_config = ConfigDict(extra="forbid")

    from_seq: int
    graph: str
    label: str | None = None
    timeout_ms: int | None = None


async def send_watch(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Watch - cdc:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    WatchRequest.model_validate(params or {})
    payload = await client._send(
        "Watch",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Watch", payload)


class RegisterTriggerRequest(BaseModel):
    """Request body for RegisterTrigger - contract/schemas/method.request.json#/methods/RegisterTrigger."""

    model_config = ConfigDict(extra="forbid")

    action_msgpack: bytes | None = None
    graph: str
    label: str | None = None
    name: str
    op: str


async def send_register_trigger(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """RegisterTrigger - cdc:admin, ControlRedb, replay OperationIdentity.

    Result: ResultPayload::String (contract/schemas/result.messaging.json#/methods/RegisterTrigger).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RegisterTriggerRequest.model_validate(params or {})
    payload = await client._send(
        "RegisterTrigger",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("RegisterTrigger", payload)


class DropTriggerRequest(BaseModel):
    """Request body for DropTrigger - contract/schemas/method.request.json#/methods/DropTrigger."""

    model_config = ConfigDict(extra="forbid")

    name: str


async def send_drop_trigger(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """DropTrigger - cdc:admin, ControlRedb, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/DropTrigger).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DropTriggerRequest.model_validate(params or {})
    payload = await client._send(
        "DropTrigger",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("DropTrigger", payload)


class ListTriggersRequest(BaseModel):
    """Request body for ListTriggers - contract/schemas/method.request.json#/methods/ListTriggers."""

    model_config = ConfigDict(extra="forbid")

    graph: str


async def send_list_triggers(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ListTriggers - cdc:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ListTriggersRequest.model_validate(params or {})
    payload = await client._send(
        "ListTriggers",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ListTriggers", payload)


class FiredTriggersRequest(BaseModel):
    """Request body for FiredTriggers - contract/schemas/method.request.json#/methods/FiredTriggers."""

    model_config = ConfigDict(extra="forbid")

    from_seq: int
    graph: str
    limit: int | None = None


async def send_fired_triggers(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FiredTriggers - cdc:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    FiredTriggersRequest.model_validate(params or {})
    payload = await client._send(
        "FiredTriggers",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FiredTriggers", payload)


class CepSubscribeRequest(BaseModel):
    """Request body for CepSubscribe - contract/schemas/method.request.json#/methods/CepSubscribe."""

    model_config = ConfigDict(extra="forbid")

    buffer: int | None = None
    pattern_msgpack: bytes


async def send_cep_subscribe(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """CepSubscribe - cep:admin, ControlRedb, replay OperationIdentity.

    Result: ResultPayload::Count (contract/schemas/result.messaging.json#/methods/CepSubscribe).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CepSubscribeRequest.model_validate(params or {})
    payload = await client._send(
        "CepSubscribe",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_count("CepSubscribe", payload)


class CepPollRequest(BaseModel):
    """Request body for CepPoll - contract/schemas/method.request.json#/methods/CepPoll."""

    model_config = ConfigDict(extra="forbid")

    sub_id: int
    timeout_ms: int | None = None


async def send_cep_poll(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CepPoll - cep:read, None, replay NotReplayable.

    Result: unclassified - the contract declares no result for this method.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CepPollRequest.model_validate(params or {})
    payload = await client._send(
        "CepPoll",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CepPoll", payload)


class CepUnsubscribeRequest(BaseModel):
    """Request body for CepUnsubscribe - contract/schemas/method.request.json#/methods/CepUnsubscribe."""

    model_config = ConfigDict(extra="forbid")

    sub_id: int


async def send_cep_unsubscribe(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """CepUnsubscribe - cep:admin, ControlRedb, replay OperationIdentity.

    Result: ResultPayload::Bool (contract/schemas/result.messaging.json#/methods/CepUnsubscribe).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CepUnsubscribeRequest.model_validate(params or {})
    payload = await client._send(
        "CepUnsubscribe",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_bool("CepUnsubscribe", payload)
