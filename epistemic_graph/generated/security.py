# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated security engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import (
    OpaqueResult,
    expect_string,
)


class GetLedgerRequest(BaseModel):
    """Request body for GetLedger - contract/schemas/method.request.json#/methods/GetLedger."""

    model_config = ConfigDict(extra="forbid")

    # Method::GetLedger carries no request fields.


async def send_get_ledger(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetLedger - ledger:read, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetLedgerRequest.model_validate(params or {})
    payload = await client._send(
        "GetLedger",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetLedger", payload)


class AuditVerifyRequest(BaseModel):
    """Request body for AuditVerify - contract/schemas/method.request.json#/methods/AuditVerify."""

    model_config = ConfigDict(extra="forbid")

    # Method::AuditVerify carries no request fields.


async def send_audit_verify(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """AuditVerify - security:audit, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    AuditVerifyRequest.model_validate(params or {})
    payload = await client._send(
        "AuditVerify",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("AuditVerify", payload)


class AuditProveInclusionRequest(BaseModel):
    """Request body for AuditProveInclusion - contract/schemas/method.request.json#/methods/AuditProveInclusion."""

    model_config = ConfigDict(extra="forbid")

    anchor_seq: Any | None = None
    node_id: str


async def send_audit_prove_inclusion(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """AuditProveInclusion - security:audit, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    AuditProveInclusionRequest.model_validate(params or {})
    payload = await client._send(
        "AuditProveInclusion",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("AuditProveInclusion", payload)


class RegisterIdentityRequest(BaseModel):
    """Request body for RegisterIdentity - contract/schemas/method.request.json#/methods/RegisterIdentity."""

    model_config = ConfigDict(extra="forbid")

    agent_id: str
    role: Any
    roles: list[str]
    signature: str
    teams: list[str]


async def send_register_identity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """RegisterIdentity - security:admin, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RegisterIdentityRequest.model_validate(params or {})
    payload = await client._send(
        "RegisterIdentity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return expect_string("RegisterIdentity", payload)


class RbacAdminRequest(BaseModel):
    """Request body for RbacAdmin - contract/schemas/method.request.json#/methods/RbacAdmin."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_rbac_admin(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RbacAdmin - security:admin, ControlRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RbacAdminRequest.model_validate(params or {})
    payload = await client._send(
        "RbacAdmin",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RbacAdmin", payload)


class GetIdentityRequest(BaseModel):
    """Request body for GetIdentity - contract/schemas/method.request.json#/methods/GetIdentity."""

    model_config = ConfigDict(extra="forbid")

    agent_id: str


async def send_get_identity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """GetIdentity - security:admin, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    GetIdentityRequest.model_validate(params or {})
    payload = await client._send(
        "GetIdentity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("GetIdentity", payload)
