# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated transactions engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import OpaqueResult


class BatchUpdateRequest(BaseModel):
    """Request body for BatchUpdate - contract/schemas/method.request.json#/methods/BatchUpdate."""

    model_config = ConfigDict(extra="forbid")

    operations_msgpack: bytes


async def send_batch_update(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BatchUpdate - node:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BatchUpdateRequest.model_validate(params or {})
    payload = await client._send(
        "BatchUpdate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BatchUpdate", payload)


class MultiGraphBatchUpdateRequest(BaseModel):
    """Request body for MultiGraphBatchUpdate - contract/schemas/method.request.json#/methods/MultiGraphBatchUpdate."""

    model_config = ConfigDict(extra="forbid")

    batches_msgpack: bytes


async def send_multi_graph_batch_update(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MultiGraphBatchUpdate - node:write, ControlRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MultiGraphBatchUpdateRequest.model_validate(params or {})
    payload = await client._send(
        "MultiGraphBatchUpdate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MultiGraphBatchUpdate", payload)


class ApplyChangeEnvelopeRequest(BaseModel):
    """Request body for ApplyChangeEnvelope - contract/schemas/method.request.json#/methods/ApplyChangeEnvelope."""

    model_config = ConfigDict(extra="forbid")

    envelope: Any


async def send_apply_change_envelope(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ApplyChangeEnvelope - ingest:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ApplyChangeEnvelopeRequest.model_validate(params or {})
    payload = await client._send(
        "ApplyChangeEnvelope",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ApplyChangeEnvelope", payload)


class ApplyChangeEnvelopesRequest(BaseModel):
    """Request body for ApplyChangeEnvelopes - contract/schemas/method.request.json#/methods/ApplyChangeEnvelopes."""

    model_config = ConfigDict(extra="forbid")

    envelopes: list[Any]


async def send_apply_change_envelopes(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ApplyChangeEnvelopes - ingest:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ApplyChangeEnvelopesRequest.model_validate(params or {})
    payload = await client._send(
        "ApplyChangeEnvelopes",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ApplyChangeEnvelopes", payload)


class ApplyMultisigMutationRequest(BaseModel):
    """Request body for ApplyMultisigMutation - contract/schemas/method.request.json#/methods/ApplyMultisigMutation."""

    model_config = ConfigDict(extra="forbid")

    mutation_type: str
    query: str
    signatures: list[str]
    threshold: int


async def send_apply_multisig_mutation(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ApplyMultisigMutation - security:admin, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ApplyMultisigMutationRequest.model_validate(params or {})
    payload = await client._send(
        "ApplyMultisigMutation",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ApplyMultisigMutation", payload)


class BeginTxnRequest(BaseModel):
    """Request body for BeginTxn - contract/schemas/method.request.json#/methods/BeginTxn."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    isolation: Any | None = None


async def send_begin_txn(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """BeginTxn - txn:control, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BeginTxnRequest.model_validate(params or {})
    payload = await client._send(
        "BeginTxn",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnAddNodeRequest(BaseModel):
    """Request body for TxnAddNode - contract/schemas/method.request.json#/methods/TxnAddNode."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    node_id: str
    properties_msgpack: bytes
    txn_id: str


async def send_txn_add_node(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnAddNode - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnAddNodeRequest.model_validate(params or {})
    payload = await client._send(
        "TxnAddNode",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnRemoveNodeRequest(BaseModel):
    """Request body for TxnRemoveNode - contract/schemas/method.request.json#/methods/TxnRemoveNode."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    node_id: str
    txn_id: str


async def send_txn_remove_node(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnRemoveNode - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnRemoveNodeRequest.model_validate(params or {})
    payload = await client._send(
        "TxnRemoveNode",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnAddEdgeRequest(BaseModel):
    """Request body for TxnAddEdge - contract/schemas/method.request.json#/methods/TxnAddEdge."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    properties_msgpack: bytes
    source_id: str
    target_id: str
    txn_id: str


async def send_txn_add_edge(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnAddEdge - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnAddEdgeRequest.model_validate(params or {})
    payload = await client._send(
        "TxnAddEdge",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnRemoveEdgeRequest(BaseModel):
    """Request body for TxnRemoveEdge - contract/schemas/method.request.json#/methods/TxnRemoveEdge."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    source_id: str
    target_id: str
    txn_id: str


async def send_txn_remove_edge(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnRemoveEdge - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnRemoveEdgeRequest.model_validate(params or {})
    payload = await client._send(
        "TxnRemoveEdge",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnCasRequest(BaseModel):
    """Request body for TxnCas - contract/schemas/method.request.json#/methods/TxnCas."""

    model_config = ConfigDict(extra="forbid")

    conditions_msgpack: bytes
    graph: Any | None = None
    node_id: str
    txn_id: str
    updates_msgpack: bytes


async def send_txn_cas(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnCas - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnCasRequest.model_validate(params or {})
    payload = await client._send(
        "TxnCas",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnAddEmbeddingRequest(BaseModel):
    """Request body for TxnAddEmbedding - contract/schemas/method.request.json#/methods/TxnAddEmbedding."""

    model_config = ConfigDict(extra="forbid")

    embedding: list[float]
    graph: Any | None = None
    node_id: str
    txn_id: str


async def send_txn_add_embedding(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnAddEmbedding - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnAddEmbeddingRequest.model_validate(params or {})
    payload = await client._send(
        "TxnAddEmbedding",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnBlobRefRequest(BaseModel):
    """Request body for TxnBlobRef - contract/schemas/method.request.json#/methods/TxnBlobRef."""

    model_config = ConfigDict(extra="forbid")

    digest: str
    graph: Any | None = None
    node_id: str
    txn_id: str


async def send_txn_blob_ref(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnBlobRef - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnBlobRefRequest.model_validate(params or {})
    payload = await client._send(
        "TxnBlobRef",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnAddMeasurementRequest(BaseModel):
    """Request body for TxnAddMeasurement - contract/schemas/method.request.json#/methods/TxnAddMeasurement."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    points: bytes
    series: str
    txn_id: str


async def send_txn_add_measurement(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnAddMeasurement - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnAddMeasurementRequest.model_validate(params or {})
    payload = await client._send(
        "TxnAddMeasurement",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnAxiomRequest(BaseModel):
    """Request body for TxnAxiom - contract/schemas/method.request.json#/methods/TxnAxiom."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    turtle: str
    txn_id: str


async def send_txn_axiom(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnAxiom - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnAxiomRequest.model_validate(params or {})
    payload = await client._send(
        "TxnAxiom",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnConstructRequest(BaseModel):
    """Request body for TxnConstruct - contract/schemas/method.request.json#/methods/TxnConstruct."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    sparql: str
    txn_id: str


async def send_txn_construct(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnConstruct - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnConstructRequest.model_validate(params or {})
    payload = await client._send(
        "TxnConstruct",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnPlanWritebackRequest(BaseModel):
    """Request body for TxnPlanWriteback - contract/schemas/method.request.json#/methods/TxnPlanWriteback."""

    model_config = ConfigDict(extra="forbid")

    anchor_id: str
    graph: Any | None = None
    plan: Any
    relationship: str
    txn_id: str


async def send_txn_plan_writeback(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnPlanWriteback - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnPlanWritebackRequest.model_validate(params or {})
    payload = await client._send(
        "TxnPlanWriteback",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TxnMaterializeBeliefRequest(BaseModel):
    """Request body for TxnMaterializeBelief - contract/schemas/method.request.json#/methods/TxnMaterializeBelief."""

    model_config = ConfigDict(extra="forbid")

    graph: Any | None = None
    node_id: str
    txn_id: str


async def send_txn_materialize_belief(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """TxnMaterializeBelief - txn:write, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TxnMaterializeBeliefRequest.model_validate(params or {})
    payload = await client._send(
        "TxnMaterializeBelief",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class CommitRequest(BaseModel):
    """Request body for Commit - contract/schemas/method.request.json#/methods/Commit."""

    model_config = ConfigDict(extra="forbid")

    idempotency_key: Any | None = None
    txn_id: str


async def send_commit(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Commit - txn:control, ControlRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CommitRequest.model_validate(params or {})
    payload = await client._send(
        "Commit",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Commit", payload)


class RollbackRequest(BaseModel):
    """Request body for Rollback - contract/schemas/method.request.json#/methods/Rollback."""

    model_config = ConfigDict(extra="forbid")

    txn_id: str


async def send_rollback(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """Rollback - txn:control, ControlRedb, replay OperationIdentity.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RollbackRequest.model_validate(params or {})
    payload = await client._send(
        "Rollback",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload
