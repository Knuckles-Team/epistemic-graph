# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated storage engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict, Field

from ._runtime import OpaqueResult


class ToMsgpackRequest(BaseModel):
    """Request body for ToMsgpack - contract/schemas/method.request.json#/methods/ToMsgpack."""

    model_config = ConfigDict(extra="forbid")

    # Method::ToMsgpack carries no request fields.


async def send_to_msgpack(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ToMsgpack - graph:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ToMsgpackRequest.model_validate(params or {})
    payload = await client._send(
        "ToMsgpack",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ToMsgpack", payload)


class FromMsgpackRequest(BaseModel):
    """Request body for FromMsgpack - contract/schemas/method.request.json#/methods/FromMsgpack."""

    model_config = ConfigDict(extra="forbid")

    msgpack: bytes


async def send_from_msgpack(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """FromMsgpack - graph:admin, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    FromMsgpackRequest.model_validate(params or {})
    payload = await client._send(
        "FromMsgpack",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class ClearLedgerRequest(BaseModel):
    """Request body for ClearLedger - contract/schemas/method.request.json#/methods/ClearLedger."""

    model_config = ConfigDict(extra="forbid")

    # Method::ClearLedger carries no request fields.


async def send_clear_ledger(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """ClearLedger - ledger:admin, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ClearLedgerRequest.model_validate(params or {})
    payload = await client._send(
        "ClearLedger",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class ApplyLedgerRequest(BaseModel):
    """Request body for ApplyLedger - contract/schemas/method.request.json#/methods/ApplyLedger."""

    model_config = ConfigDict(extra="forbid")

    transactions: list[str]


async def send_apply_ledger(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """ApplyLedger - ledger:write, GraphRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ApplyLedgerRequest.model_validate(params or {})
    payload = await client._send(
        "ApplyLedger",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BackupRequest(BaseModel):
    """Request body for Backup - contract/schemas/method.request.json#/methods/Backup."""

    model_config = ConfigDict(extra="forbid")

    destination: str
    label: Any | None = None


async def send_backup(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Backup - admin:backup, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    BackupRequest.model_validate(params or {})
    payload = await client._send(
        "Backup",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Backup", payload)


class RestoreRequest(BaseModel):
    """Request body for Restore - contract/schemas/method.request.json#/methods/Restore."""

    model_config = ConfigDict(extra="forbid")

    source: str
    target_shards: int


async def send_restore(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Restore - admin:backup, ControlRedb, replay OperationIdentity.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RestoreRequest.model_validate(params or {})
    payload = await client._send(
        "Restore",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Restore", payload)


class TsAppendRequest(BaseModel):
    """Request body for TsAppend - contract/schemas/method.request.json#/methods/TsAppend."""

    model_config = ConfigDict(extra="forbid")

    bucket_ns: int
    field_names: list[str] | None = None
    n_fields: int
    points_msgpack: bytes
    series_id: str


async def send_ts_append(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """TsAppend - timeseries:write, SeriesRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TsAppendRequest.model_validate(params or {})
    payload = await client._send(
        "TsAppend",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TsRangeRequest(BaseModel):
    """Request body for TsRange - contract/schemas/method.request.json#/methods/TsRange."""

    model_config = ConfigDict(extra="forbid")

    from_: int = Field(..., alias="from")
    series_id: str
    to: int


async def send_ts_range(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """TsRange - timeseries:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TsRangeRequest.model_validate(params or {})
    payload = await client._send(
        "TsRange",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("TsRange", payload)


class TsAsofJoinRequest(BaseModel):
    """Request body for TsAsofJoin - contract/schemas/method.request.json#/methods/TsAsofJoin."""

    model_config = ConfigDict(extra="forbid")

    left_ts_msgpack: bytes
    series_id: str
    tolerance: int | None = None


async def send_ts_asof_join(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """TsAsofJoin - timeseries:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TsAsofJoinRequest.model_validate(params or {})
    payload = await client._send(
        "TsAsofJoin",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("TsAsofJoin", payload)


class TsWindowRequest(BaseModel):
    """Request body for TsWindow - contract/schemas/method.request.json#/methods/TsWindow."""

    model_config = ConfigDict(extra="forbid")

    agg: str
    from_: int = Field(..., alias="from")
    series_id: str
    to: int
    width: int


async def send_ts_window(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """TsWindow - timeseries:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TsWindowRequest.model_validate(params or {})
    payload = await client._send(
        "TsWindow",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("TsWindow", payload)


class TsGapFillRequest(BaseModel):
    """Request body for TsGapFill - contract/schemas/method.request.json#/methods/TsGapFill."""

    model_config = ConfigDict(extra="forbid")

    from_: int = Field(..., alias="from")
    series_id: str
    step: int
    to: int


async def send_ts_gap_fill(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """TsGapFill - timeseries:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TsGapFillRequest.model_validate(params or {})
    payload = await client._send(
        "TsGapFill",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("TsGapFill", payload)


class TsEvictRequest(BaseModel):
    """Request body for TsEvict - contract/schemas/method.request.json#/methods/TsEvict."""

    model_config = ConfigDict(extra="forbid")

    cutoff: int
    series_id: str


async def send_ts_evict(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """TsEvict - timeseries:write, SeriesRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TsEvictRequest.model_validate(params or {})
    payload = await client._send(
        "TsEvict",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TsDeleteSeriesRequest(BaseModel):
    """Request body for TsDeleteSeries - contract/schemas/method.request.json#/methods/TsDeleteSeries."""

    model_config = ConfigDict(extra="forbid")

    series_id: str


async def send_ts_delete_series(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """TsDeleteSeries - timeseries:write, SeriesRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    TsDeleteSeriesRequest.model_validate(params or {})
    payload = await client._send(
        "TsDeleteSeries",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class TsListSeriesRequest(BaseModel):
    """Request body for TsListSeries - contract/schemas/method.request.json#/methods/TsListSeries."""

    model_config = ConfigDict(extra="forbid")

    # Method::TsListSeries carries no request fields.


async def send_ts_list_series(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """TsListSeries - timeseries:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    TsListSeriesRequest.model_validate(params or {})
    payload = await client._send(
        "TsListSeries",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("TsListSeries", payload)


class BlobBeginRequest(BaseModel):
    """Request body for BlobBegin - contract/schemas/method.request.json#/methods/BlobBegin."""

    model_config = ConfigDict(extra="forbid")

    chunk_size: int | None = None


async def send_blob_begin(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """BlobBegin - blob:write, BlobRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BlobBeginRequest.model_validate(params or {})
    payload = await client._send(
        "BlobBegin",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BlobChunkPutRequest(BaseModel):
    """Request body for BlobChunkPut - contract/schemas/method.request.json#/methods/BlobChunkPut."""

    model_config = ConfigDict(extra="forbid")

    cursor: int
    data: bytes


async def send_blob_chunk_put(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """BlobChunkPut - blob:write, BlobRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BlobChunkPutRequest.model_validate(params or {})
    payload = await client._send(
        "BlobChunkPut",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BlobCommitRequest(BaseModel):
    """Request body for BlobCommit - contract/schemas/method.request.json#/methods/BlobCommit."""

    model_config = ConfigDict(extra="forbid")

    cursor: int


async def send_blob_commit(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> str:
    """BlobCommit - blob:write, BlobRedb, replay OperationIdentity.

    Result: typed ResultPayload::String (contract/schemas/result.String.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BlobCommitRequest.model_validate(params or {})
    payload = await client._send(
        "BlobCommit",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BlobFetchBeginRequest(BaseModel):
    """Request body for BlobFetchBegin - contract/schemas/method.request.json#/methods/BlobFetchBegin."""

    model_config = ConfigDict(extra="forbid")

    digest: str


async def send_blob_fetch_begin(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BlobFetchBegin - blob:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    BlobFetchBeginRequest.model_validate(params or {})
    payload = await client._send(
        "BlobFetchBegin",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BlobFetchBegin", payload)


class BlobChunkGetRequest(BaseModel):
    """Request body for BlobChunkGet - contract/schemas/method.request.json#/methods/BlobChunkGet."""

    model_config = ConfigDict(extra="forbid")

    cursor: int
    idx: int


async def send_blob_chunk_get(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BlobChunkGet - blob:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    BlobChunkGetRequest.model_validate(params or {})
    payload = await client._send(
        "BlobChunkGet",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BlobChunkGet", payload)


class BlobFetchEndRequest(BaseModel):
    """Request body for BlobFetchEnd - contract/schemas/method.request.json#/methods/BlobFetchEnd."""

    model_config = ConfigDict(extra="forbid")

    cursor: int


async def send_blob_fetch_end(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> bool:
    """BlobFetchEnd - blob:read, None, replay NotReplayable.

    Result: typed ResultPayload::Bool (contract/schemas/result.Bool.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    BlobFetchEndRequest.model_validate(params or {})
    payload = await client._send(
        "BlobFetchEnd",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BlobRefRequest(BaseModel):
    """Request body for BlobRef - contract/schemas/method.request.json#/methods/BlobRef."""

    model_config = ConfigDict(extra="forbid")

    digest: str


async def send_blob_ref(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """BlobRef - blob:write, BlobRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BlobRefRequest.model_validate(params or {})
    payload = await client._send(
        "BlobRef",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BlobUnrefRequest(BaseModel):
    """Request body for BlobUnref - contract/schemas/method.request.json#/methods/BlobUnref."""

    model_config = ConfigDict(extra="forbid")

    digest: str


async def send_blob_unref(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> int:
    """BlobUnref - blob:write, BlobRedb, replay OperationIdentity.

    Result: typed ResultPayload::Count (contract/schemas/result.Count.json).
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BlobUnrefRequest.model_validate(params or {})
    payload = await client._send(
        "BlobUnref",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return payload


class BlobGcRequest(BaseModel):
    """Request body for BlobGc - contract/schemas/method.request.json#/methods/BlobGc."""

    model_config = ConfigDict(extra="forbid")

    # Method::BlobGc carries no request fields.


async def send_blob_gc(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """BlobGc - blob:admin, BlobRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    BlobGcRequest.model_validate(params or {})
    payload = await client._send(
        "BlobGc",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("BlobGc", payload)


class ImportSqliteFileRequest(BaseModel):
    """Request body for ImportSqliteFile - contract/schemas/method.request.json#/methods/ImportSqliteFile."""

    model_config = ConfigDict(extra="forbid")

    path: str


async def send_import_sqlite_file(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ImportSqliteFile - admin:sqlite-file, ControlRedb, replay OperationIdentity.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ImportSqliteFileRequest.model_validate(params or {})
    payload = await client._send(
        "ImportSqliteFile",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ImportSqliteFile", payload)


class ExportSqliteFileRequest(BaseModel):
    """Request body for ExportSqliteFile - contract/schemas/method.request.json#/methods/ExportSqliteFile."""

    model_config = ConfigDict(extra="forbid")

    path: str
    tables: list[str] | None = None


async def send_export_sqlite_file(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ExportSqliteFile - admin:sqlite-file, None, replay NotReplayable.

    Result: opaque (Json) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ExportSqliteFileRequest.model_validate(params or {})
    payload = await client._send(
        "ExportSqliteFile",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ExportSqliteFile", payload)
