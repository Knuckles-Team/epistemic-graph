# @generated by `cargo run -p eg-capabilities --features contract --bin gen_contract`.
# Source of truth: crates/eg-capabilities/src/domains/*.rs. Do NOT edit by hand.
"""Generated coordination engine-contract client surface."""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict

from ._runtime import (
    OpaqueResult,
)


class ClaimNextRequest(BaseModel):
    """Request body for ClaimNext - contract/schemas/method.request.json#/methods/ClaimNext."""

    model_config = ConfigDict(extra="forbid")

    label: str
    updates_msgpack: bytes


async def send_claim_next(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ClaimNext - node:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ClaimNextRequest.model_validate(params or {})
    payload = await client._send(
        "ClaimNext",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ClaimNext", payload)


class ClaimWorkItemRequest(BaseModel):
    """Request body for ClaimWorkItem - contract/schemas/method.request.json#/methods/ClaimWorkItem."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_claim_work_item(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ClaimWorkItem - work:claim, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ClaimWorkItemRequest.model_validate(params or {})
    payload = await client._send(
        "ClaimWorkItem",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ClaimWorkItem", payload)


class AcquireCapacityRequest(BaseModel):
    """Request body for AcquireCapacity - contract/schemas/method.request.json#/methods/AcquireCapacity."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_acquire_capacity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """AcquireCapacity - capacity:lease, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    AcquireCapacityRequest.model_validate(params or {})
    payload = await client._send(
        "AcquireCapacity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("AcquireCapacity", payload)


class RenewCapacityRequest(BaseModel):
    """Request body for RenewCapacity - contract/schemas/method.request.json#/methods/RenewCapacity."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_renew_capacity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RenewCapacity - capacity:lease, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RenewCapacityRequest.model_validate(params or {})
    payload = await client._send(
        "RenewCapacity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RenewCapacity", payload)


class ReleaseCapacityRequest(BaseModel):
    """Request body for ReleaseCapacity - contract/schemas/method.request.json#/methods/ReleaseCapacity."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_release_capacity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReleaseCapacity - capacity:lease, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReleaseCapacityRequest.model_validate(params or {})
    payload = await client._send(
        "ReleaseCapacity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReleaseCapacity", payload)


class ReclaimExpiredCapacityRequest(BaseModel):
    """Request body for ReclaimExpiredCapacity - contract/schemas/method.request.json#/methods/ReclaimExpiredCapacity."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_reclaim_expired_capacity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReclaimExpiredCapacity - capacity:lease, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReclaimExpiredCapacityRequest.model_validate(params or {})
    payload = await client._send(
        "ReclaimExpiredCapacity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReclaimExpiredCapacity", payload)


class ReconcileCapacityRequest(BaseModel):
    """Request body for ReconcileCapacity - contract/schemas/method.request.json#/methods/ReconcileCapacity."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_reconcile_capacity(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReconcileCapacity - capacity:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ReconcileCapacityRequest.model_validate(params or {})
    payload = await client._send(
        "ReconcileCapacity",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReconcileCapacity", payload)


class CapacityStatusRequest(BaseModel):
    """Request body for CapacityStatus - contract/schemas/method.request.json#/methods/CapacityStatus."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_capacity_status(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CapacityStatus - capacity:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    CapacityStatusRequest.model_validate(params or {})
    payload = await client._send(
        "CapacityStatus",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CapacityStatus", payload)


class UpdateCapacityCellRequest(BaseModel):
    """Request body for UpdateCapacityCell - contract/schemas/method.request.json#/methods/UpdateCapacityCell."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_update_capacity_cell(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """UpdateCapacityCell - capacity:admin, GraphRedb, replay OperationIdentity.

    Result: opaque (Raw) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    UpdateCapacityCellRequest.model_validate(params or {})
    payload = await client._send(
        "UpdateCapacityCell",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("UpdateCapacityCell", payload)


class KgDelegateRequest(BaseModel):
    """Request body for KgDelegate - contract/schemas/method.request.json#/methods/KgDelegate."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_kg_delegate(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """KgDelegate - work:delegate, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    KgDelegateRequest.model_validate(params or {})
    payload = await client._send(
        "KgDelegate",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("KgDelegate", payload)


class SubmitWorkItemRequest(BaseModel):
    """Request body for SubmitWorkItem - contract/schemas/method.request.json#/methods/SubmitWorkItem."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_submit_work_item(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """SubmitWorkItem - work:submit, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    SubmitWorkItemRequest.model_validate(params or {})
    payload = await client._send(
        "SubmitWorkItem",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("SubmitWorkItem", payload)


class SubmitWorkItemsRequest(BaseModel):
    """Request body for SubmitWorkItems - contract/schemas/method.request.json#/methods/SubmitWorkItems."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_submit_work_items(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """SubmitWorkItems - work:submit, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    SubmitWorkItemsRequest.model_validate(params or {})
    payload = await client._send(
        "SubmitWorkItems",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("SubmitWorkItems", payload)


class MintWorkItemClaimCapabilityRequest(BaseModel):
    """Request body for MintWorkItemClaimCapability - contract/schemas/method.request.json#/methods/MintWorkItemClaimCapability."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_mint_work_item_claim_capability(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """MintWorkItemClaimCapability - work:claim-capability, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    MintWorkItemClaimCapabilityRequest.model_validate(params or {})
    payload = await client._send(
        "MintWorkItemClaimCapability",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("MintWorkItemClaimCapability", payload)


class VerifyWorkItemClaimCapabilityRequest(BaseModel):
    """Request body for VerifyWorkItemClaimCapability - contract/schemas/method.request.json#/methods/VerifyWorkItemClaimCapability."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_verify_work_item_claim_capability(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """VerifyWorkItemClaimCapability - work:claim-capability, GraphRedb, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    VerifyWorkItemClaimCapabilityRequest.model_validate(params or {})
    payload = await client._send(
        "VerifyWorkItemClaimCapability",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("VerifyWorkItemClaimCapability", payload)


class RenewWorkItemLeaseRequest(BaseModel):
    """Request body for RenewWorkItemLease - contract/schemas/method.request.json#/methods/RenewWorkItemLease."""

    model_config = ConfigDict(extra="forbid")

    fencing_token: int
    lease_epoch: int
    lease_ms: int
    now_ms: int
    tenant: str
    work_item_id: str
    worker_id: str


async def send_renew_work_item_lease(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RenewWorkItemLease - work:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RenewWorkItemLeaseRequest.model_validate(params or {})
    payload = await client._send(
        "RenewWorkItemLease",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RenewWorkItemLease", payload)


class CommitWorkItemResultRequest(BaseModel):
    """Request body for CommitWorkItemResult - contract/schemas/method.request.json#/methods/CommitWorkItemResult."""

    model_config = ConfigDict(extra="forbid")

    error_ref: Any | None = None
    fencing_token: int
    idempotency_key: str
    lease_epoch: int
    now_ms: int
    outcome: str
    outcome_extension: Any | None = None
    result_ref: Any | None = None
    retryable: bool | None = None
    tenant: str
    work_item_id: str
    worker_id: str


async def send_commit_work_item_result(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CommitWorkItemResult - work:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CommitWorkItemResultRequest.model_validate(params or {})
    payload = await client._send(
        "CommitWorkItemResult",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CommitWorkItemResult", payload)


class CancelWorkItemRequest(BaseModel):
    """Request body for CancelWorkItem - contract/schemas/method.request.json#/methods/CancelWorkItem."""

    model_config = ConfigDict(extra="forbid")

    idempotency_key: str
    now_ms: int
    reason_ref: Any | None = None
    tenant: str
    work_item_id: str


async def send_cancel_work_item(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CancelWorkItem - work:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CancelWorkItemRequest.model_validate(params or {})
    payload = await client._send(
        "CancelWorkItem",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CancelWorkItem", payload)


class DeferWorkItemRequest(BaseModel):
    """Request body for DeferWorkItem - contract/schemas/method.request.json#/methods/DeferWorkItem."""

    model_config = ConfigDict(extra="forbid")

    fencing_token: int
    idempotency_key: str
    lease_epoch: int
    next_retry_at_ms: int
    now_ms: int
    reason_ref: Any | None = None
    tenant: str
    work_item_id: str
    worker_id: str


async def send_defer_work_item(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DeferWorkItem - work:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    DeferWorkItemRequest.model_validate(params or {})
    payload = await client._send(
        "DeferWorkItem",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DeferWorkItem", payload)


class CasWorkItemMetadataRequest(BaseModel):
    """Request body for CasWorkItemMetadata - contract/schemas/method.request.json#/methods/CasWorkItemMetadata."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_cas_work_item_metadata(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CasWorkItemMetadata - work:write, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CasWorkItemMetadataRequest.model_validate(params or {})
    payload = await client._send(
        "CasWorkItemMetadata",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CasWorkItemMetadata", payload)


class ReserveWorkItemResourcesRequest(BaseModel):
    """Request body for ReserveWorkItemResources - contract/schemas/method.request.json#/methods/ReserveWorkItemResources."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_reserve_work_item_resources(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReserveWorkItemResources - resource:reserve, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReserveWorkItemResourcesRequest.model_validate(params or {})
    payload = await client._send(
        "ReserveWorkItemResources",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReserveWorkItemResources", payload)


class ReleaseWorkItemResourcesRequest(BaseModel):
    """Request body for ReleaseWorkItemResources - contract/schemas/method.request.json#/methods/ReleaseWorkItemResources."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_release_work_item_resources(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReleaseWorkItemResources - resource:reserve, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReleaseWorkItemResourcesRequest.model_validate(params or {})
    payload = await client._send(
        "ReleaseWorkItemResources",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReleaseWorkItemResources", payload)


class ReclaimWorkItemResourcesRequest(BaseModel):
    """Request body for ReclaimWorkItemResources - contract/schemas/method.request.json#/methods/ReclaimWorkItemResources."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_reclaim_work_item_resources(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReclaimWorkItemResources - resource:reserve, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReclaimWorkItemResourcesRequest.model_validate(params or {})
    payload = await client._send(
        "ReclaimWorkItemResources",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReclaimWorkItemResources", payload)


class QueryWorkItemReservationRequest(BaseModel):
    """Request body for QueryWorkItemReservation - contract/schemas/method.request.json#/methods/QueryWorkItemReservation."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_query_work_item_reservation(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """QueryWorkItemReservation - resource:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    QueryWorkItemReservationRequest.model_validate(params or {})
    payload = await client._send(
        "QueryWorkItemReservation",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("QueryWorkItemReservation", payload)


class ResourceReservationStatusRequest(BaseModel):
    """Request body for ResourceReservationStatus - contract/schemas/method.request.json#/methods/ResourceReservationStatus."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_resource_reservation_status(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ResourceReservationStatus - resource:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ResourceReservationStatusRequest.model_validate(params or {})
    payload = await client._send(
        "ResourceReservationStatus",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ResourceReservationStatus", payload)


class UpdateResourceHostRequest(BaseModel):
    """Request body for UpdateResourceHost - contract/schemas/method.request.json#/methods/UpdateResourceHost."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_update_resource_host(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """UpdateResourceHost - resource:host, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    UpdateResourceHostRequest.model_validate(params or {})
    payload = await client._send(
        "UpdateResourceHost",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("UpdateResourceHost", payload)


class ReserveDevelopmentLaneRequest(BaseModel):
    """Request body for ReserveDevelopmentLane - contract/schemas/method.request.json#/methods/ReserveDevelopmentLane."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_reserve_development_lane(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ReserveDevelopmentLane - lane:reserve, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ReserveDevelopmentLaneRequest.model_validate(params or {})
    payload = await client._send(
        "ReserveDevelopmentLane",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ReserveDevelopmentLane", payload)


class RenewDevelopmentLaneRequest(BaseModel):
    """Request body for RenewDevelopmentLane - contract/schemas/method.request.json#/methods/RenewDevelopmentLane."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_renew_development_lane(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """RenewDevelopmentLane - lane:reserve, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    RenewDevelopmentLaneRequest.model_validate(params or {})
    payload = await client._send(
        "RenewDevelopmentLane",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("RenewDevelopmentLane", payload)


class ObserveDevelopmentLaneRequest(BaseModel):
    """Request body for ObserveDevelopmentLane - contract/schemas/method.request.json#/methods/ObserveDevelopmentLane."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_observe_development_lane(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ObserveDevelopmentLane - lane:reserve, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    ObserveDevelopmentLaneRequest.model_validate(params or {})
    payload = await client._send(
        "ObserveDevelopmentLane",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ObserveDevelopmentLane", payload)


class FinishDevelopmentLaneRequest(BaseModel):
    """Request body for FinishDevelopmentLane - contract/schemas/method.request.json#/methods/FinishDevelopmentLane."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_finish_development_lane(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """FinishDevelopmentLane - lane:reserve, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    FinishDevelopmentLaneRequest.model_validate(params or {})
    payload = await client._send(
        "FinishDevelopmentLane",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("FinishDevelopmentLane", payload)


class CleanupDevelopmentLaneRequest(BaseModel):
    """Request body for CleanupDevelopmentLane - contract/schemas/method.request.json#/methods/CleanupDevelopmentLane."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_cleanup_development_lane(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """CleanupDevelopmentLane - lane:cleanup, GraphRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    CleanupDevelopmentLaneRequest.model_validate(params or {})
    payload = await client._send(
        "CleanupDevelopmentLane",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("CleanupDevelopmentLane", payload)


class QueryDevelopmentLaneRequest(BaseModel):
    """Request body for QueryDevelopmentLane - contract/schemas/method.request.json#/methods/QueryDevelopmentLane."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_query_development_lane(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """QueryDevelopmentLane - lane:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    QueryDevelopmentLaneRequest.model_validate(params or {})
    payload = await client._send(
        "QueryDevelopmentLane",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("QueryDevelopmentLane", payload)


class DevelopmentLaneStatusRequest(BaseModel):
    """Request body for DevelopmentLaneStatus - contract/schemas/method.request.json#/methods/DevelopmentLaneStatus."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_development_lane_status(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """DevelopmentLaneStatus - lane:read, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    DevelopmentLaneStatusRequest.model_validate(params or {})
    payload = await client._send(
        "DevelopmentLaneStatus",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("DevelopmentLaneStatus", payload)


class UpdateDevelopmentLaneQuotaRequest(BaseModel):
    """Request body for UpdateDevelopmentLaneQuota - contract/schemas/method.request.json#/methods/UpdateDevelopmentLaneQuota."""

    model_config = ConfigDict(extra="forbid")

    request: Any


async def send_update_development_lane_quota(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """UpdateDevelopmentLaneQuota - lane:quota, GraphRedb, replay OperationIdentity.

    Result: opaque (Raw) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    UpdateDevelopmentLaneQuotaRequest.model_validate(params or {})
    payload = await client._send(
        "UpdateDevelopmentLaneQuota",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("UpdateDevelopmentLaneQuota", payload)


class ResourceStatsPageRequest(BaseModel):
    """Request body for ResourceStatsPage - contract/schemas/method.request.json#/methods/ResourceStatsPage."""

    model_config = ConfigDict(extra="forbid")

    cursor: Any | None = None
    limit: int | None = None
    summary: bool | None = None


async def send_resource_stats_page(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """ResourceStatsPage - service:control, None, replay NotReplayable.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED.
    """
    ResourceStatsPageRequest.model_validate(params or {})
    payload = await client._send(
        "ResourceStatsPage",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("ResourceStatsPage", payload)


class AnalyticsJobRequest(BaseModel):
    """Request body for AnalyticsJob - contract/schemas/method.request.json#/methods/AnalyticsJob."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_analytics_job(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """AnalyticsJob - jobs:write, JobsRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    AnalyticsJobRequest.model_validate(params or {})
    payload = await client._send(
        "AnalyticsJob",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("AnalyticsJob", payload)


class StatechartRequest(BaseModel):
    """Request body for Statechart - contract/schemas/method.request.json#/methods/Statechart."""

    model_config = ConfigDict(extra="forbid")

    op: Any


async def send_statechart(
    client: Any,
    params: dict[str, Any] | None = None,
    graph: str | None = None,
    *,
    idempotency_key: str | None = None,
) -> OpaqueResult:
    """Statechart - statechart:write, StatechartRedb, replay OperationIdentity.

    Result: opaque (Undeclared) - the contract declares no result schema.
    Errors: INVALID_ARGUMENT, ACCESS_DENIED, CONFLICT, IDEMPOTENCY_CONFLICT, REDIRECTED, READ_ONLY.
    """
    StatechartRequest.model_validate(params or {})
    payload = await client._send(
        "Statechart",
        params,
        graph,
        idempotency_key=idempotency_key,
    )
    return OpaqueResult("Statechart", payload)
