# (file, pattern) pairs already reviewed and confirmed NOT a live credential.
# Generated during the release.yml/advisory.yml CI redesign, 2026-08-09,
# against a HEAD~300..HEAD scan of this repo's real history (697,592 added
# lines) run once to sanity-check this newly-wired gate before it was made
# release-blocking. Format: <file>\t<pattern> per line. D-CIP-14 ratchet: a
# pair here never blocks; anything NOT here fails closed. Keyed on
# (file, pattern), not commit -- the scanned range is normally just the
# incremental push delta (github.event.before..HEAD), so this baseline
# exists as a safety net for an unusual wide-range scan (e.g. a squash-merge
# push or the gate's first-ever run), not because it is expected to matter
# on a typical push.
#
# Fix or retire an entry by adding an inline '# sanitizer:ignore' marker to
# the offending line instead (the convention this gate already honors) and
# removing the row here; re-run --base <old-range> to confirm removal
# doesn't reintroduce a hit.

# Rust-side synthetic test/fixture secrets -- string literals used as stand-in
# credentials in unit tests and roundtrip harnesses, never read from a real
# credential source.
b/src/raft/harness/cluster.rs	generic_secret_assignment
b/crates/eg-plan/src/federation.rs	generic_secret_assignment
b/crates/eg-plan/src/federation_sql_tests.rs	generic_secret_assignment
b/crates/eg-plan/src/federation_sql_tests.rs	db_url_with_password
b/src/server/dataset_handle.rs	generic_secret_assignment
b/src/server/kvcache_http/mod.rs	generic_secret_assignment
b/src/server/mysql_wire/auth.rs	generic_secret_assignment
b/tests/pgwire_roundtrip.rs	generic_secret_assignment

# Documented placeholder DSN shapes in doc comments / wire-type docstrings and
# the SQL-federation test fixture DSNs -- illustrative `postgres://user:pw@...`
# examples, not resolvable credentials.
b/crates/eg-types/src/wire.rs	db_url_with_password
b/epistemic_graph/client.py	db_url_with_password

# Python-side synthetic test secrets.
b/tests/test_isolation.py	generic_secret_assignment
b/tests/test_kvcache_connector.py	generic_secret_assignment
b/tests/test_service_layer.py	generic_secret_assignment
