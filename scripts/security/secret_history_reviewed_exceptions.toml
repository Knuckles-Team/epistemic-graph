# Reviewed register of scripts/security/check_secret_history.py hits that are
# confirmed NOT live secrets, for a commit that already landed on `main`
# before this repo's first-ever push -- so the underlying patch cannot be
# rewritten without invalidating every SHA recorded in the refactor-program
# checkpoint/ledger and forcing every live lane to rebase (operator ruling,
# EH-313, 2026-09-19).
#
# This is NOT a blanket suppression. Modeled directly on
# dupehound-distinct.toml's precedent (EH-264/EH-268):
#
# - Each entry is scoped to ONE exact (commit, file, pattern, content-digest)
#   tuple -- never the file, never the pattern globally, and never the gate.
#   All four fields must match a specific `credentialHits` finding exactly,
#   including `context_digest` (sha256 of the finding's own `context` text,
#   computed the same way `check_secret_history._context_digest` computes it
#   from a live scan) -- pinned the same way dupehound pins a function's
#   normalized-text digest: change the underlying content at all and the
#   digest stops matching, the entry goes stale, and the finding returns.
# - Recording an entry here does NOT license any future match of the same
#   pattern -- not in the same file, not in any other file, not in the next
#   commit to touch this same file. Every other hit is still a hard failure.
#   Proven mechanically, not just asserted: `check_secret_history.py
#   --self-check` plants a second, differently-shaped hit of the very same
#   pattern in a later commit and confirms it is still caught even with an
#   exception entry present.
# - Nothing here is written or updated by a tool.

[[exception]]
commit = "ab66a0a7d1439bd44cceb706a205fe3ef50bae46"
file = "b/scripts/check_tracked_privacy.py"
pattern = "db_url_with_password"
context_digest = "sha256:5c42f1c424932eccdd9b5c1e90406f0ee231cb2fb5bba7fee80f115bd13923f0"
reviewed_on = "2026-09-19"
reason = """
`postgresql://agent:agent@localhost:5432/agent_kg` is a documented example
INSIDE scripts/check_tracked_privacy.py -- the pattern-definition file of
this repo's own privacy checker -- illustrating the DSN shape that file's
_CREDENTIAL_PLACEHOLDER_TOKENS allowance exists to recognize. It points at
`localhost` (never a reachable remote host in any deployment of this repo),
and its placeholder username is identical to its placeholder password
("agent" == "agent") -- not a credential to anything that exists; there is
no live database, account, or service this string could authenticate
against. The HEAD occurrence of this exact example is already fixed
(commit ad95e53e71ca57a9a11ff3ed719e05569027fda8 rewords it to a
scheme-free `<scheme>://<user>:<password>@<host>...` template that none of
this gate's 19 credential patterns can match -- verified directly against
CREDENTIAL_PATTERNS before that commit landed). This entry covers ONLY the
immutable historical `+` line already committed at ab66a0a7d (2026-09-13),
which predates that fix and cannot be edited without rewriting already-
landed `main` history (invalidating every downstream lane's rebase point).
This entry does NOT license any future match of `db_url_with_password` --
in this file, in any other file, or in any other commit. A new occurrence
of a credential-shaped DSN anywhere else in the scanned range fails this
gate exactly as before; see check_secret_history.py --self-check for a
mechanical proof that a second, differently-shaped hit of this same pattern
is still caught with this entry in place.
"""
