# Facade-full tests known to FAIL, accepted as tracked debt.
# Generated by `scripts/cargo_test_ratchet.py --update-baseline`.
# RATCHET: the gate fails on any failing test NOT listed here.
# This list must only ever SHRINK.
#
# 2026-08-08: 78 -> 55 after fixing test_state()'s `persistence: None`
# (939 tests both runs, so the comparison is like-for-like). The
# remaining 55 are REAL fixture gaps now that tests reach the true
# authorization paths: graph ACLs for the test principal, and the
# EG-KG.ontology.rdf-update-guard integrity requirement.
#
# 2026-08-14: 55 -> 0. Re-validated every entry against a CI-faithful
# unconstrained `cargo test -p epistemic-graph --features full --no-fail-fast`
# run on R710: 54 of the 55 already PASS (fixed by later commits -- the B-8/B-9
# durability-contract rebase and the coalescer stats fix -- without the
# baseline ever being re-shrunk). The 55th, `server::mqtt_wire::tests::
# eg281_listener_connect_subscribe_publish_deliver_roundtrip`, was diagnosed as
# GOC-70 (scheduling-dependent, not a product defect) and fixed structurally.
# The RATCHET IS NOW RETIRED (see .pre-commit-config.yaml's `cargo-test-full`
# hook, which runs the same absolute-green command as release.yml's
# "Test (facade full)" step instead of scripts/cargo_test_ratchet.py). This
# file is kept empty, not deleted, so a future legitimate red-main episode can
# reactivate the same differential-gating shape by re-running
# `python3 scripts/cargo_test_ratchet.py --update-baseline` and re-wiring the
# hook -- see that script's module docstring.
